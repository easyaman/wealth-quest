# WEALTH QUEST — Art Direction: Low Poly · Flat Shaded 3D

> เอกสารทิศทางงานอาร์ต v1 (18 ส.ค. 2026) — อ้างอิงภาพ `IMG_3632.jpg` (หน้าโชว์อาวุธ low poly บนแท่นหมุน)
> ใช้คู่กับ `WEALTH-QUEST-GDD.md` บทที่ 3A, 12 และ `godot/CLAUDE.md`
> **เอกสารนี้แทนที่แผน "สไปรต์ 8 ทิศ × 16 อาชีพ" ใน GDD 12.3 และความเสี่ยงข้อแรกในบทที่ 17**

---

## 0. สรุปการตัดสินใจ

| หัวข้อ | ตัดสินใจ |
|---|---|
| สไตล์ | **Low poly, flat shaded** — หน้าเรียบ ไม่มี smooth normal, สีทึบต่อหน้า, ไม่มี texture ยกเว้น palette strip |
| เทคนิค | **3D จริงใน Godot 4** — กล้อง `Camera3D` แบบ orthographic มุม isometric (yaw 45°, pitch ~30°) ฉากเมืองทั้งฉากเป็นเมช |
| เหตุผลหลัก | ตัด "หลุมงบประมาณอันดับ 1" ของ GDD ทิ้ง — โมเดลตัวละคร 1 ตัว หมุนได้ทุกทิศ ไม่ต้องวาดสไปรต์ 8 ทิศ × 16 อาชีพ อีกต่อไป · เพิ่มอาชีพ/พาหนะ/ทรัพย์สินทีหลังได้ด้วยการเปลี่ยนสีและสลับชิ้นส่วน |
| ขอบเขต | ฉากเมือง 10 สถานที่ · ตัวละคร 1 ริก + 16 ชุดอาชีพ · พาหนะ 5 · อุปกรณ์ 2 · ไอคอนทรัพย์สิน ~35 (เป็นเมช 3D โชว์บนแท่นหมุน) · ความฝัน 6 · UI kit แบน |
| สิ่งที่ **ไม่เปลี่ยน** | `core/` ไม่รับรู้เรื่องอาร์ตเลย · ทุกอย่างอ่านสถานะจากสัญญาณ `changed` / `month_ended` เท่านั้น · Godot ยังต้องรัน headless sim ได้เหมือนเดิม |

---

## 1. Visual Pillars

1. **"ของเล่นบนโต๊ะ"** — ทุกอย่างดูเหมือนโมเดลไม้ทาสีวางบนแท่น เห็นเหลี่ยม เห็นหน้า ไม่มีความสมจริง
   ตรงกับโทนเกม: จำลองชีวิตแบบกระดาน ไม่ใช่ซิมชีวิตจริง
2. **สีบอกความหมาย ไม่ใช่ตกแต่ง** — เงิน = ทอง/เหลืองอำพัน · เวลา = ฟ้า · สุขภาพ = ชมพู-แดง
   ใช้สามสีนี้ซ้ำทั้งใน HUD, วัตถุในฉาก, และเอฟเฟกต์ (สอดคล้อง GDD 1.1 ข้อ 5 "อ่านรู้เรื่องใน 10 วินาที")
3. **โชว์ของแบบในรูปอ้างอิง** — ทุกสิ่งที่ผู้เล่น "ซื้อ" (พาหนะ อุปกรณ์ ทรัพย์สิน ความฝัน) ต้องมีฉากโชว์บนแท่นหมุน
   พร้อมแถบสถิติแบนใต้ภาพ เพราะเกมนี้ขายการตัดสินใจซื้อ — ของต้องดูน่าอยาก แล้วให้ตัวเลขเป็นคนเบรก
4. **เงียบเมื่อไม่มีอะไรเกิด, ดังเมื่อมีเรื่อง** — ฉากปกติสีเย็น นิ่ง · Payday/ปิดดีล/ภัยพิบัติ ถึงจะมีแสง สี และการเคลื่อนไหวเข้ามา

---

## 2. Style Spec (สำหรับคนปั้นและสำหรับ shader)

### 2.1 เมช
- **Poly budget:** prop เล็ก 50–300 tris · พาหนะ 300–900 · อาคาร 400–1,500 · ตัวละคร 800–1,500 (รวมชุด)
- **Flat shading เท่านั้น** — export จาก Blender ด้วย *Shade Flat* + `Auto Smooth` ปิด (หรือแตก normal ต่อหน้า)
- ไม่มี bevel นอกจากขอบใหญ่ที่จงใจ (เช่น มุมโต๊ะ มุมตึก) — bevel 1 ชั้นพอ
- ไม่มี UV texture รายชิ้น — ใช้ **palette texture 1 แผ่น (256×16 px)** ทุกเมชในเกม map UV เป็นจุดบนแผ่นนี้
  (วิธี "gradient/palette atlas" มาตรฐานของ low poly) → เปลี่ยนสีทั้งเกมได้จากไฟล์เดียว
- ทุกโมเดล origin อยู่ที่ฐาน (พื้น) · หน่วย 1 unit = 1 เมตร · หันหน้า −Z
- ฟอร์แมต: `.glb` (glTF binary) วางใน `godot/world/models/<กลุ่ม>/<id>.glb` **ชื่อไฟล์ = id ใน `data/*.json`**
  (เช่น `vehicles/car_used.glb`, `places/bank.glb`) เพื่อให้โค้ดโหลดจาก id ได้โดยไม่ต้อง map ด้วยมือ

### 2.2 แสงและเงา
- `DirectionalLight3D` ดวงเดียว จากมุมบนซ้าย-หน้า (ตามรูปอ้างอิง แสงหลักมาจากบนซ้าย) + `WorldEnvironment` ambient สีฟ้าอ่อน
- เงา: เปิด shadow ของแสงหลักดวงเดียว soft-ish (PCF 13) — เงาแข็งเกินไปจะทำให้เห็นเหลี่ยมมากจนรก
- ไม่ใช้ SSAO, SSR, bloom หนัก — อนุญาต bloom เบาเฉพาะวัตถุ emissive (ป้ายนีออน, แสง Payday)
- Fog เชิงเส้นสีเดียวกับพื้นหลัง เพื่อละลายขอบฉากเมืองแบบในรูป (พื้นหลังฟุ้งเป็นแถบสี ไม่มีขอบฟ้าชัด)

### 2.3 กล้อง
- **ฉากเมือง:** `Camera3D` projection = Orthogonal, size ≈ 22, rotation `(-30°, 45°, 0)` · เลื่อนตาม x ของสถานที่ (0–100 ใน `places.json` → −25..+25 units)
  แผนที่ใน GDD เป็นเส้นตรง (x เดียว) — วางอาคารเรียงตามถนนสายเดียว โค้งเล็กน้อยเพื่อไม่ให้ดูเป็นแถวตรงเป๊ะ
- **แท่นโชว์ (Showcase):** perspective FOV 35°, วัตถุอยู่กลาง, หมุนรอบแกน Y ช้าๆ 12°/วิ, ลากเมาส์เพื่อหมุนเองได้
  พื้นหลัง: gradient สองสีของ "กลุ่มของ" (พาหนะ = ฟ้าเข้ม, ทรัพย์สิน = เขียวอมเทา, ความฝัน = ม่วงทอง) + กรอบสี่เหลี่ยมมนซ้อนหลังตามรูป
- Render ที่ 1920×1080, MSAA 4× (low poly ต้องมี AA ไม่งั้นขอบเป็นบันได)

### 2.4 พาเลตหลัก (ใส่ใน `godot/ui/theme/palette.gd` และ palette texture)

| ชื่อ | HEX | ใช้กับ |
|---|---|---|
| `bg_deep` | `#0A1420` | พื้นหลัง UI (ตรงกับ `main.gd` ปัจจุบัน) |
| `bg_scene` | `#1B2D5B` → `#3D5BA9` | gradient ฉากโชว์ (ตามรูปอ้างอิง) |
| `money` | `#F2B233` | เงินสด, ดีล, Payday |
| `money_dark` | `#B87A12` | หนี้, ดอกเบี้ย |
| `time` | `#4FC3F7` | แถบเวลา, ชั่วโมงบนปุ่ม |
| `health` | `#F06292` | แถบสุขภาพ |
| `danger` | `#E53935` | ภัยพิบัติ, ล้มละลาย |
| `win` | `#66BB6A` | ประกาศชัยชนะ, ผลตอบแทน + |
| `neutral_1..5` | `#F5F1E8 · #C9C2B4 · #8B8578 · #4A463F · #23211D` | ไม้/ปูน/พื้น/เงา |
| `accent_wood` | `#7A3B2E` | ไม้แดง (เหมือนพานท้ายปืนในรูป) — ของ "แพง/พรีเมียม" |
| `accent_steel` | `#3B4A5C` | โลหะเทา-น้ำเงิน — ของ "ใช้งาน/เครื่องจักร" |

กฎ: สีตัวละคร/สถานที่ต้องไม่ใช้ `money`/`time`/`health` เป็นสีหลัก เพื่อกันสีบอกสถานะสับสน

### 2.5 UI 2D ที่วางทับ 3D
- แบนสนิท ไม่มี gradient/เงาตกใน UI (ให้ 3D เป็นคนมีมิติ)
- แถบสถิติแบบรูปอ้างอิง: เส้นบางสีเทา + เส้นเติมสีขาว/สีตามความหมาย ตัวหนังสือพิมพ์ใหญ่ตัวเดียว
  → ใช้เป็นมาตรฐาน `stat_bar` ของทั้งเกม (แถบเวลา สุขภาพ ผลตอบแทน)
- ฟอนต์: sans-serif หัวตัด (ไทย+อังกฤษ) — ✅ **ฝังแล้ว**: Noto Sans Thai (SIL OFL 1.1) อยู่ที่
  `godot/ui/theme/fonts/` เป็นฟอนต์ variable ไฟล์เดียวได้ทั้งตัวปกติและตัวหนา (ดู README ในโฟลเดอร์นั้น)
- ไอคอน: เลิกใช้อีโมจิใน UI จริง → เปลี่ยนเป็น **ภาพเรนเดอร์จากเมช 3D ตัวเดียวกัน** (ถ่ายจากมุมเดียวกันทุกชิ้น 30° pitch, 128×128, พื้นโปร่ง) ทำอัตโนมัติด้วยสคริปต์ (ดู 4.3)

---

## 3. รายการงานอาร์ต (นับชิ้นจริงจาก data)

| กลุ่ม | จำนวน | ที่มาของรายการ | หมายเหตุ |
|---|---|---|---|
| **สถานที่ / อาคาร** | 10 | `places.json` — บ้าน ออฟฟิศ ธนาคาร ศูนย์อสังหาฯ ร้านทอง co-working สถาบันสอน ฟิตเนส ห้าง โรงแรม | อาคารละ 1 เมช + ป้ายชื่อ + prop รอบตึก 2–3 ชิ้นใช้ซ้ำได้ (ต้นไม้ เสาไฟ ม้านั่ง รถจอด) |
| **ถนน/พื้น/สภาพอากาศ** | 1 ชุด | GDD 3A, 8.3 | พื้นเป็นแผ่น modular · ภัยพิบัติ 3 สภาพ: ฝนตกน้ำท่วม (แผ่นน้ำโปร่งขึ้นสูง), เศรษฐกิจตก (ฟ้าเทา ป้ายลดราคา), โรคระบาด (คนน้อย หน้ากาก) |
| **ตัวละครผู้เล่น** | 1 ริก + 16 ชุด | `jobs.json` 16 อาชีพ | ริกเดียว หัวกลม/ทรงกล่อง ไม่มีหน้า (มีแค่จุดตา) · ชุดอาชีพ = สลับเมชลำตัว+หมวก+ของถือ ผ่านสี palette · แอนิเมชัน 6: idle, walk, work, tired (สุขภาพ<40), celebrate, hit |
| **ผู้เล่นคนอื่น/AI** | 0 เพิ่ม | ใช้ริกเดิม เปลี่ยนสีเสื้อตาม slot | ตอน AI เดินในฉากให้จางลง 60% ไม่แย่งความสนใจ |
| **พาหนะ** | 5 | `places.json` → vehicles: bus, motorbike, car_used, car_new, car_luxury | รถหรูต้องดู "แพงเกินเหตุ" ชัดเจน (สีเงา ปีก) เพราะเป็นกับดักที่ตั้งใจ |
| **อุปกรณ์** | 2 | phone, laptop | โชว์บนแท่น + วางในบ้าน |
| **ทรัพย์สิน (ไอคอน 3D)** | ~35 | `deals.json` templates + ประเภทใน GDD 5.2 | 11 ประเภทหลัก × 2–4 variant — เช่น ธุรกิจจิ๋ว = รถเข็น/ตู้เต่าบิน/แผงลอย · อสังหาฯ = ตึกแถว/คอนโด/อพาร์ตเมนต์ · เก็งกำไร = ทองแท่ง/พระเครื่อง/นาฬิกา/เหรียญ · กองทุน = กราฟบนแผ่นกระดาน |
| **ความฝัน** | 6 | `dreams.json` | ชิ้นใหญ่สุด รายละเอียดสูงสุดในเกม (แต่ยัง flat) — บ้านริมทะเล, ลูกโลก+กระเป๋าเดินทาง, อาคารมูลนิธิ, ศูนย์สุขภาพ, รถ 3 คัน+บ้าน, จรวด/ตึกบริษัท |
| **แพ็กเกจฟิตเนส/รีสอร์ต** | 6 | `places.json` | ใช้ prop เล็ก: ตั๋ว ดัมเบล เหรียญเทรนเนอร์ / ร่ม เตียงชายหาด เครื่องบิน |
| **UI kit** | 1 ชุด | GDD 12 | ปุ่ม 3 ระดับ, การ์ดดีล, stat_bar, แบนเนอร์, หน้าต่างโมดัล, ตัวเลขทอย |
| **VFX** | 4 | GDD 12.3 | Payday (เหรียญเหลี่ยมกระเด็น), ปิดดีล (ของโผล่บนแท่น + แสงวาบ), ภัยพิบัติ (จอสั่น + สีเปลี่ยน), ชนะ (คอนเฟตติเหลี่ยม) — ทั้งหมดเป็น `GPUParticles3D` เมช low poly |

**ลำดับทำ (ตามความถี่ที่ผู้เล่นเห็น — GDD 12.3):** UI kit + stat_bar → ไอคอนทรัพย์สิน 3D + Showcase → พาหนะ/อุปกรณ์ → ฉากเมือง 10 อาคาร → ตัวละคร → ความฝัน → VFX

---

## 4. โครงสร้างใน Godot

```
godot/
├── world/                              # ฉาก 3D — อ่าน core ผ่านสัญญาณเท่านั้น
│   ├── city/
│   │   ├── City.tscn                   # Node3D: กล้อง iso + แสง + อาคารตาม places.json
│   │   ├── city.gd                     # bind(match) → วาง PlaceNode ตาม x · ฟัง disaster_started
│   │   ├── place_node.gd               # อาคาร 1 หลัง: โหลด models/places/<id>.glb, ป้าย, จุดยืน, ไฮไลต์เมื่อ hover
│   │   └── avatar.gd                   # ตัวละคร: bind(player) → เดินไป place เมื่อ player.place เปลี่ยน
│   ├── showcase/
│   │   ├── Showcase.tscn               # SubViewport + แท่นหมุน + gradient bg + stat bars
│   │   └── showcase.gd                 # show(kind, id, stats:[{label, value, max}]) — ใช้ซ้ำทุกที่
│   ├── models/                         # .glb ทั้งหมด ชื่อไฟล์ = id ใน data/*.json
│   │   ├── places/  vehicles/  devices/  assets/  dreams/  packs/  props/  character/
│   ├── materials/
│   │   ├── palette.png                 # 256×16 palette atlas
│   │   ├── flat.tres                   # StandardMaterial3D: albedo = palette.png, roughness 1, specular 0, unshaded=false
│   │   └── flat_emissive.tres
│   ├── vfx/                            # payday.tscn, deal_closed.tscn, disaster.tscn, win.tscn
│   └── tools/
│       └── icon_bake.gd                # headless: เรนเดอร์ทุก .glb เป็น icons/<id>.png 128×128
├── ui/theme/
│   ├── palette.gd                      # const สี ตามข้อ 2.4
│   ├── theme.tres
│   └── icons/                          # ผลลัพธ์จาก icon_bake — ห้ามแก้มือ
```

### 4.1 กฎเชื่อมกับ core (สืบทอดกฎเหล็กเดิม)
- `world/` **อ่าน** `WQPlayer` / `WQMatch` ได้ แต่ **ห้ามแก้ค่า** — ทุกการกระทำของผู้เล่นยังไปผ่านปุ่มใน `ui/` แล้ว `ui/` เรียก core · `world/` แค่แสดงผล
  (คลิกอาคารในฉาก = ส่งสัญญาณ `place_clicked(place_id)` ให้ `ui/main.gd` ตัดสินใจ ไม่เรียก `travel_to()` เอง)
- ตัวละครเดินไปอาคารเมื่อ `player.place` เปลี่ยน — animation ล้วน ไม่มีผลต่อสถานะเกม · ต้องข้ามได้ (คลิกซ้ำ = วาร์ป)
- ห้าม `Math.random()`/`randf()` ที่กระทบเกม — ใน `world/` ใช้ `randf()` ได้เฉพาะแอนิเมชัน (ใบไม้ไหว, คอนเฟตติ)
- headless sim ต้องรันได้โดยไม่โหลด `world/` เลย (ห้าม `preload` จาก core หรือ sim)

### 4.2 Showcase — จุดที่เอาแรงบันดาลใจจากรูปอ้างอิงมาใช้ตรงที่สุด
ใช้ที่: การ์ดดีล (hover/คลิกเพื่อดูใหญ่) · ร้านพาหนะ/อุปกรณ์ที่ห้าง · ทอยความฝันตอนเข้าด่าน 2 · หน้าเลือกอาชีพ (โชว์ตัวละครในชุด)
แถบสถิติใต้ของ (แบบ FIREPOWER/RANGE ในรูป) map กับตัวเลขที่ GDD 12.2 บังคับให้เห็น:

| ของ | แถบ |
|---|---|
| ดีล | ผลตอบแทน %/เดือน · เงินดาวน์ · เวลาปิดดีล (ชม.) · ความผันผวน |
| พาหนะ | ตัวคูณเวลาเดินทาง · ค่าใช้จ่าย/เดือน · เวลาที่ได้คืน/เดือน (คำนวณจาก commute จริงของผู้เล่น) |
| แพ็กเกจ | เวลา (ชม.) · สุขภาพ + · ลดโอกาสเรื่องร้าย % |
| ความฝัน | ต้องมีสุทธิ ×N · ต้องมีรายได้ ×N · คุณอยู่ที่ % แล้ว |

### 4.3 icon_bake — ไอคอนไม่ต้องวาดมือ
สคริปต์ headless วนทุก `.glb` → วางบนฉากมาตรฐาน (แสงเดิม กล้อง 30°/45°) → `get_texture().get_image().save_png()`
รันใหม่ทุกครั้งที่โมเดลเปลี่ยน · ผลลัพธ์ commit เข้า repo เพื่อให้ UI ใช้ได้โดยไม่ต้องมี 3D โหลดตอน headless

---

## 5. Pipeline สร้างโมเดล (คนเดียวทำได้)

1. **Blender** (ฟรี) — box modeling + palette UV · หรือ **Blockbench** สำหรับ prop เล็กและตัวละครทรงกล่อง (เร็วกว่ามาก, export glTF ได้)
2. ทางลัดที่ยอมรับได้: ใช้ AI text-to-3D / image-to-3D สร้าง blockout แล้ว **decimate + flat shade + re-palette ใน Blender** — ทุกชิ้นต้องผ่านมือคนให้เข้า palette เดียวกัน ไม่งั้นสไตล์จะแตก
3. Asset pack CC0 ที่สไตล์ตรง (เช่น Kenney) ใช้เป็น prop รอบฉากได้ แต่ **อาคารหลัก 10 หลัง พาหนะ ความฝัน ทำเอง** เพื่อให้เป็นเอกลักษณ์
4. Checklist ก่อน export ทุกชิ้น: origin ที่ฐาน · scale apply แล้ว · flat shade · material เดียวชื่อ `flat` · tris ไม่เกิน budget · ชื่อไฟล์ = id
5. เปิดใน Godot ผ่าน `Showcase.tscn` เทียบกับรูปอ้างอิง — ถ้าดู "เรียบเกิน" ให้เพิ่มรายละเอียดเป็น **ชิ้นแยก** (สกรู แถบ ป้าย) ไม่ใช่เพิ่ม subdivision

---

## 6. สิ่งที่ต้องอัปเดตในเอกสารอื่น
- GDD หน้าแรก: "2.5D — สไปรต์บนฉาก isometric" → "3D low poly flat-shaded, กล้อง orthographic isometric"
- GDD 12.3 และ 17: ลบแถว "ตัวละคร 8 ทิศ" · แทนด้วย "ริก 1 ตัว + 16 ชุด"
- `godot/CLAUDE.md`: เพิ่มหัวข้อ "งานอาร์ต 3D" (ทำแล้วในคอมมิตนี้)
- README หลัก: บรรทัด `godot/` เพิ่ม "world/ = ฉาก 3D low poly"

> เนื่องจาก GDD สร้างอัตโนมัติจาก `writegdd.js` — แก้ที่ `writegdd.js` แล้วรัน `node writegdd.js` ห้ามแก้ GDD มือ
