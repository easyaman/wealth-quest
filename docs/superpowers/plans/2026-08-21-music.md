# แผนลงมือทำเพลงพื้นหลัง Wealth Quest

> **สำหรับเอเจนต์ที่มาทำงานนี้:** REQUIRED SUB-SKILL — ใช้ `superpowers:subagent-driven-development`
> (แนะนำ) หรือ `superpowers:executing-plans` ทำทีละ task ตามลำดับ · ทุกสเต็ปเป็น checkbox

**Goal:** เกมมีเพลงพื้นหลังสามเพลงที่วนไม่มีรอยต่อ เปลี่ยนตามสถานการณ์ด้วยการเฟดไขว้ ปรับ/ปิดได้จากแผงเสียง

**Architecture:** `WQMusic` เก็บเพลงเป็นตารางโน้ตแบบ tracker แล้วเรียบเรียงเป็น PCM ชุดเดียวโดยเรียก
`WQSynth` ต่อโน้ต (แบ่งหน้าที่แบบเดียวกับ `WQBank`+`WQSynth` ของเสียงสั้น) · `audio/tools/music_bake.gd`
อบเป็น `.wav` พร้อมตราประทับ sha256 กันทับงานคนทำเพลง · `WQAudio` ได้เลนที่สาม: บัส `Music`
ตัวเล่นสองตัวสลับกันเฟดไขว้ และนโยบายเลือกเพลงจากสถานะผู้เล่น (ไม่ใช่จาก `ui/`)

**Tech Stack:** Godot 4.7.1 · GDScript · `AudioStreamWAV` (16-bit PCM 22050 Hz mono) · `Tween`

**Spec:** `docs/superpowers/specs/2026-08-21-music-design.md`

## Global Constraints

- **ห้ามแตะ `core/` แม้แต่บรรทัดเดียว** — เพลงอ่านสถานะที่มีอยู่แล้วล้วนๆ
- **อัตราสุ่ม 22050 Hz mono 16-bit** (`WQSynth.RATE`) เท่ากับเสียงสั้น — 43 KB ต่อวินาที
- **`WQMusic.render()` ต้องได้ไบต์เดิมเป๊ะทุกครั้ง** — ตราประทับ sha256 ทั้งท่อขึ้นกับข้อนี้
- **เฟดไขว้ 0.8 วินาที** (`WQAudio.FADE`) · **เกณฑ์วิกฤต `health < 40`** เท่ากับเสียง `health_low`
- **เสียงเหตุการณ์มาจากสัญญาณเท่านั้น** (กฎเหล็กข้อ 6 ใน `godot/CLAUDE.md`) — `ui/` สั่งเลือกเพลงไม่ได้
- **โหมด `--script` ไม่รู้จักชื่อ autoload** — เรียก `WQAudio` ผ่านชื่อ `class_name` เท่านั้น
- **headless ไม่ยิงเสียงจริง** (`_silent`) แต่ต้องบันทึกสถานะให้เทสต์อ่านได้เสมอ
- **ทุกเทสต์ต้องพิสูจน์ด้วยการทำลายโค้ดให้ล้มก่อน** แล้วคืนโค้ดเดิม — เขียวโดยไม่รู้ว่าตรวจอะไร = ไม่มีเทสต์
- รันทุกอย่างจาก `godot/` เสมอ · หลังอบไฟล์ใหม่ต้อง `godot --headless --path . --import`

---

### Task 1: `WQMusic` — ตารางโน้ต + ตัวเรียบเรียง (ยังไม่มีไฟล์ ยังไม่มีเสียงในเกม)

**Files:**
- Create: `godot/audio/music.gd`
- Create: `godot/sim/music_check.gd`

**Interfaces:**
- Consumes: `WQSynth.RATE` · `WQSynth.render(spec: Array) -> PackedByteArray`
- Produces:
  - `WQMusic.TRACKS: Dictionary` — คีย์คือ id เพลง
  - `WQMusic.ids() -> Array` · `WQMusic.cells(line: String) -> PackedStringArray`
  - `WQMusic.bars(line: String) -> Array` · `WQMusic.note_hz(name: String) -> float`
  - `WQMusic.length_sec(id: String) -> float` · `WQMusic.render(id: String) -> PackedByteArray`
  - `WQMusic.stream(id: String) -> AudioStreamWAV` (ตั้งจุดลูปไว้ให้แล้ว)

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มก่อน — `godot/sim/music_check.gd`**

```gdscript
extends SceneTree
## ตรวจระบบเพลงพื้นหลังแบบ headless
##   godot --headless --path . --script res://sim/music_check.gd
##
## แยกจาก audio_check เพราะนั่นยาว 795 บรรทัดแล้ว และ sim/ แยกสูทตามระบบอยู่แล้ว
## ที่นี่ตรวจได้แค่ "โครงถูกไหม · มีเสียงออกไหม · เลือกเพลงถูกตัวไหม"
## ไม่ได้ตรวจว่าเพราะไหม — อันนั้นต้องเปิดฟังเอง: WQ_MUSIC=phase1 godot --path .

const MUSIC_DIR := "res://audio/music"
const ALL_CHECKS: Array[String] = ["tracks"]

var _fails := 0
## เช็กไหนที่รันจนจบฟังก์ชันจริง — กันสูทเขียวปลอมตอน SCRIPT ERROR หลุดกลางฟังก์ชัน
## (กติกาเดียวกับ sim/audio_check.gd ซึ่งเคยเจอเคสนี้จริงมาแล้ว)
var _completed := {}


func _init() -> void:
	_check_tracks()
	for name in ALL_CHECKS:
		if not _completed.get(name, false):
			_fails += 1
			print("  ❌ เช็ก \"%s\" ไม่รันจบฟังก์ชัน — สคริปต์พังกลางทางก่อนถึงเครื่องหมายจบ" % name)
	print("music_check: %s" % ("ผ่านทั้งหมด ✅" if _fails == 0 else "ไม่ผ่าน %d ข้อ ❌" % _fails))
	quit(1 if _fails > 0 else 0)


## ตารางโน้ตของทุกเพลงต้องมีโครงตรงกันทุกช่อง ไม่งั้นทำนองกับเบสจะเหลื่อมกันทั้งเพลง
## โดยไม่มี error ให้เห็น — เป็นความผิดพลาดที่เกิดง่ายที่สุดเวลาแก้ทำนอง (ลืมเติม ".")
func _check_tracks() -> void:
	_eq("มีเพลงอย่างน้อยหนึ่งเพลง", WQMusic.ids().size() >= 1, true)

	_eq("A4 = 440 Hz", snappedf(WQMusic.note_hz("A4"), 0.01), 440.0)
	_eq("C4 = 261.63 Hz", snappedf(WQMusic.note_hz("C4"), 0.01), 261.63)
	_eq("A#3 = Bb3", snappedf(WQMusic.note_hz("A#3"), 0.01), snappedf(WQMusic.note_hz("Bb3"), 0.01))
	_eq("โน้ตสูงขึ้นหนึ่งอ็อกเทฟ = ความถี่สองเท่า",
		snappedf(WQMusic.note_hz("A5") / WQMusic.note_hz("A4"), 0.001), 2.0)

	for id in WQMusic.ids():
		var t: Dictionary = WQMusic.TRACKS[id]
		var lead_bars := WQMusic.bars(String(t["lead"]))
		for ch in ["lead", "bass", "drum"]:
			_eq("\"%s\" ช่อง %s มีจำนวนห้องเท่ากับทำนอง" % [id, ch],
				WQMusic.bars(String(t[ch])).size(), lead_bars.size())
			_eq("\"%s\" ช่อง %s มีจำนวนช่องต่อห้องตรงกับทำนองทุกห้อง" % [id, ch],
				WQMusic.bars(String(t[ch])), lead_bars)
		for b in lead_bars:
			_eq("\"%s\" ทุกห้องยาวเท่ากับ beats_per_bar" % id, b, int(t["beats_per_bar"]))

		# โทเคนที่พิมพ์ผิดต้องไม่เงียบหาย — ไม่งั้นโน้ตนั้นหายไปจากเพลงโดยไม่มีใครรู้
		for ch in ["lead", "bass"]:
			for tok in WQMusic.cells(String(t[ch])):
				if tok == "." or tok == "-": continue
				_eq("\"%s\" โน้ต \"%s\" ในช่อง %s อ่านออก" % [id, tok, ch],
					WQMusic.note_hz(tok) > 0.0, true)
		for tok in WQMusic.cells(String(t["drum"])):
			_eq("\"%s\" โทเคนกลอง \"%s\" อ่านออก" % [id, tok],
				tok == "." or tok == "-" or WQMusic.DRUM.has(tok), true)

		# ความยาวต้องลงตัวพอดีห้อง ไม่งั้นรอยต่อลูปเบี้ยว
		var cells := WQMusic.cells(String(t["lead"])).size()
		_eq("\"%s\" ความยาววินาทีตรงกับจำนวนช่อง × จังหวะ" % id,
			snappedf(WQMusic.length_sec(id), 0.0001),
			snappedf(float(cells) * 60.0 / float(t["bpm"]), 0.0001))

		var pcm := WQMusic.render(id)
		_eq("\"%s\" จำนวนแซมเปิลตรงกับความยาวเป๊ะ" % id,
			pcm.size() / 2, int(round(WQMusic.length_sec(id) * float(WQSynth.RATE))))
		_eq("\"%s\" อบซ้ำได้ไบต์เดิมเป๊ะ" % id, WQMusic.render(id) == pcm, true)

		var peak := 0
		var i := 0
		while i + 1 < pcm.size():
			peak = maxi(peak, absi(pcm.decode_s16(i)))
			i += 2
		_eq("\"%s\" ดังพอให้ได้ยิน (พีคเกิน 10%% ของเต็มสเกล)" % id, peak > 3276, true)
		_eq("\"%s\" ไม่ตัดยอดจนแตก (พีคไม่ชนเพดาน)" % id, peak < 32767, true)

		var st := WQMusic.stream(id)
		_eq("\"%s\" สตรีมตั้งลูปไว้แล้ว" % id, st.loop_mode, AudioStreamWAV.LOOP_FORWARD)
		_eq("\"%s\" จุดจบลูปอยู่ท้ายเพลงพอดี" % id, st.loop_end, pcm.size() / 2)

	_completed["tracks"] = true


func _eq(label: String, got, want) -> void:
	if got == want: return
	_fails += 1
	print("  ❌ %s: ได้ %s ต้องการ %s" % [label, str(got), str(want)])
```

- [ ] **Step 2: รันให้เห็นว่าล้มจริง**

```bash
cd godot && godot --headless --path . --script res://sim/music_check.gd
```

Expected: FAIL — `Parse Error: Identifier "WQMusic" not declared in the current scope`

- [ ] **Step 3: เขียน `godot/audio/music.gd`**

```gdscript
class_name WQMusic
extends RefCounted
## เพลงพื้นหลัง — ตารางโน้ตของทุกเพลง + ตัวเรียบเรียงที่ประกอบเป็น PCM ชุดเดียว
##
## คู่กับ WQSynth เหมือนที่ WQBank เป็นกับเสียงสั้น: WQSynth รู้จักแค่ "หนึ่งโน้ตหน้าตายังไง"
## WQMusic รู้จัก "โน้ตไหนดังตอนไหนและผสมกันยังไง" ส่วน "เพลงไหนดังตอนไหนของเกม"
## เป็นเรื่องของ WQAudio ที่เดียว — ที่นี่ไม่รู้จักเกมเลย
##
## **ต้องได้ไบต์เดิมเป๊ะทุกครั้ง** — ตัวอบใช้ sha256 แยกว่าไฟล์ไหนอบจากโค้ดไฟล์ไหนคนทำมา
## (WQSynth ใช้ RandomNumberGenerator ที่ตั้ง seed ไว้อยู่แล้ว จึงคงที่ให้เอง)
##
## รูปแบบสตริง: หนึ่งช่อง = หนึ่งจังหวะ · "|" คั่นห้อง (ให้คนอ่านและให้เทสต์ตรวจโครง)
##   "C4"  โน้ต (ตัวโน้ต + #/b + อ็อกเทฟ)      "."  เงียบ
##   "-"   ต่อเสียงจากช่องก่อนหน้า (ทำเสียงยาว)   "x"/"X"  กลองเบา/หนัก (เฉพาะช่อง drum)

## คลื่นและระดับเสียงประจำช่อง — รวมกันไม่ถึง 1.0 เพื่อให้มีที่เหลือตอนสามช่องดังพร้อมกัน
const CHANNEL_WAVE := {"lead": "square", "bass": "triangle"}
const CHANNEL_VOL := {"lead": 0.30, "bass": 0.26}

## กลองใช้ช่อง noise ของ WQSynth — f0→f1 คือความถี่ sample-and-hold ไม่ใช่ระดับเสียง
## กวาดลงเร็ว = "ตุบ" (กระเดื่อง) · ถี่คงที่สั้นๆ = "ฉึก" (ไฮแฮต)
## ความยาวกลองไม่ขึ้นกับจังหวะที่ถืออยู่ ไฮแฮตที่ลากยาวหนึ่งจังหวะเต็มไม่ใช่เสียงกลอง
const DRUM := {
	"x": {"f0": 2400.0, "f1": 1800.0, "dur": 0.10, "vol": 0.14},
	"X": {"f0": 700.0, "f1": 160.0, "dur": 0.20, "vol": 0.24},
}

const TRACKS := {
	## ด่าน 1 — "ออกจากสนามแข่งหนู" ยังไม่ชนะแต่ยังไหว: C major เดินคอร์ด C–Am–F–G
	## จังหวะปานกลาง ไม่เร่งเร้า เพราะเพลงนี้ต้องดังอยู่หลายสิบเดือนโดยไม่กวนสมาธิ
	"phase1": {
		"bpm": 110, "beats_per_bar": 4,
		"lead":
			"E4 .  G4 .  | A4 .  G4 E4 | F4 .  A4 .  | G4 -  -  .  " +
			"E4 .  G4 C5 | B4 .  A4 .  | F4 .  G4 A4 | G4 -  -  .  " +
			"C5 .  B4 .  | A4 .  G4 E4 | F4 .  E4 D4 | C4 -  -  .  " +
			"E4 .  G4 .  | A4 .  C5 .  | G4 .  F4 E4 | D4 -  -  . ",
		"bass":
			"C2 -  -  -  | A2 -  -  -  | F2 -  -  -  | G2 -  -  -  " +
			"C2 -  -  -  | A2 -  -  -  | F2 -  -  -  | G2 -  -  -  " +
			"C2 -  -  -  | A2 -  -  -  | F2 -  -  -  | G2 -  -  -  " +
			"C2 -  -  -  | A2 -  -  -  | F2 -  -  -  | G2 -  -  - ",
		"drum":
			"X  .  x  .  | X  .  x  x  | X  .  x  .  | X  .  x  X  " +
			"X  .  x  .  | X  .  x  x  | X  .  x  .  | X  .  x  X  " +
			"X  .  x  .  | X  .  x  x  | X  .  x  .  | X  .  x  X  " +
			"X  .  x  .  | X  .  x  x  | X  .  x  .  | X  .  x  X ",
	},
}


static func ids() -> Array:
	var out := TRACKS.keys()
	out.sort()
	return out


## ตัดขีดคั่นห้องออก คืนช่องทั้งหมดเรียงต่อกัน — "|" เป็นแค่เครื่องหมายให้คนอ่าน
static func cells(line: String) -> PackedStringArray:
	var out := PackedStringArray()
	for tok in line.replace("\n", " ").replace("\t", " ").split(" ", false):
		if tok != "|": out.append(tok)
	return out


## จำนวนช่องของแต่ละห้อง — เทสต์ใช้เทียบว่าทุกช่องมีโครงเดียวกันเป๊ะ
static func bars(line: String) -> Array:
	var out: Array = []
	var n := 0
	for tok in line.replace("\n", " ").replace("\t", " ").split(" ", false):
		if tok == "|":
			out.append(n)
			n = 0
		else:
			n += 1
	out.append(n)
	return out


## ชื่อโน้ต → ความถี่ (A4 = 440 Hz) · คืน 0.0 ถ้าอ่านไม่ออก ให้ผู้เรียกจัดการต่อ
static func note_hz(name: String) -> float:
	const STEP := {"C": 0, "D": 2, "E": 4, "F": 5, "G": 7, "A": 9, "B": 11}
	if name.is_empty(): return 0.0
	var letter := name.substr(0, 1).to_upper()
	if not STEP.has(letter): return 0.0
	var semi: int = STEP[letter]
	var rest := name.substr(1)
	while rest.begins_with("#") or rest.begins_with("b"):
		semi += 1 if rest.begins_with("#") else -1
		rest = rest.substr(1)
	if not rest.is_valid_int(): return 0.0
	var midi := (rest.to_int() + 1) * 12 + semi
	return 440.0 * pow(2.0, (float(midi) - 69.0) / 12.0)


static func length_sec(id: String) -> float:
	var t: Dictionary = TRACKS[id]
	return float(cells(String(t["lead"])).size()) * 60.0 / float(t["bpm"])


## เรียบเรียงทั้งเพลงเป็น PCM ชุดเดียว
##
## รางผสมเป็น int32 ไม่ใช่ int16 เพราะสามช่องบวกกันเกินช่วง 16 บิตได้ระหว่างทาง
## แล้วค่อยตัดยอดตอนแปลงกลับครั้งเดียว — ถ้าตัดยอดทีละช่องจะเพี้ยนกว่าที่ควร
static func render(id: String) -> PackedByteArray:
	var t: Dictionary = TRACKS[id]
	var spb := 60.0 / float(t["bpm"])
	var frames := int(round(length_sec(id) * float(WQSynth.RATE)))
	var mix := PackedInt32Array()
	mix.resize(frames)

	for ch in ["lead", "bass", "drum"]:
		var cs := cells(String(t[ch]))
		var i := 0
		while i < cs.size():
			var tok := cs[i]
			if tok == "." or tok == "-":
				i += 1
				continue
			var hold := 1
			while i + hold < cs.size() and cs[i + hold] == "-":
				hold += 1
			_stamp(mix, frames, ch, tok, i, float(hold) * spb, spb)
			i += hold

	var out := PackedByteArray()
	out.resize(frames * 2)
	for i in frames:
		out.encode_s16(i * 2, clampi(mix[i], -32767, 32767))
	return out


## วางโน้ตหนึ่งตัวลงบนรางผสม
##
## หางที่ล้นท้ายเพลงถูก **วนกลับไปบวกที่ต้นเพลง** ไม่ใช่ตัดทิ้ง เพราะเพลงนี้จะถูกเล่นวน
## ถ้าตัดหางทิ้งจะได้ยินเสียง "ป๊อก" ทุกครั้งที่ครบรอบ ซึ่งเป็นข้อที่แก้ทีหลังยากที่สุด
static func _stamp(mix: PackedInt32Array, frames: int, ch: String, tok: String,
		cell: int, dur: float, spb: float) -> void:
	var spec: Array = []
	if ch == "drum":
		var d: Dictionary = DRUM[tok]
		spec = ["noise", float(d["f0"]), float(d["f1"]), float(d["dur"]),
			0.002, float(d["dur"]) * 0.6, float(d["vol"])]
	else:
		var hz := note_hz(tok)
		if hz <= 0.0: return
		spec = [String(CHANNEL_WAVE[ch]), hz, hz, dur,
			0.005, minf(0.09, dur * 0.4), float(CHANNEL_VOL[ch])]

	var pcm := WQSynth.render(spec)
	var at := int(round(float(cell) * spb * float(WQSynth.RATE)))
	for i in int(pcm.size() / 2):
		mix[(at + i) % frames] += pcm.decode_s16(i * 2)


## สตรีมพร้อมเล่นวน — จุดลูปตั้งไว้ให้แล้วเพราะไฟล์ .wav ที่ Godot นำเข้าได้ loop_mode = 0 เสมอ
static func stream(id: String) -> AudioStreamWAV:
	var st := AudioStreamWAV.new()
	st.format = AudioStreamWAV.FORMAT_16_BITS
	st.mix_rate = WQSynth.RATE
	st.stereo = false
	st.data = render(id)
	st.loop_mode = AudioStreamWAV.LOOP_FORWARD
	st.loop_begin = 0
	st.loop_end = int(st.data.size() / 2)
	return st
```

- [ ] **Step 4: รันให้ผ่าน**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/music_check.gd
```

Expected: `music_check: ผ่านทั้งหมด ✅`

- [ ] **Step 5: พิสูจน์ว่าเทสต์จับของจริง — ทำลายทีละจุด แล้วคืนโค้ดเดิมทุกครั้ง**

ทำทีละข้อ รันสูทหลังแก้ทุกครั้ง แล้ว `git checkout godot/audio/music.gd` คืนก่อนข้อถัดไป:

| ทำลาย | เทสต์ที่ต้องล้ม |
|---|---|
| ลบ `.` ตัวหนึ่งออกจากท้ายห้องแรกของช่อง `bass` | จำนวนช่องต่อห้องตรงกับทำนองทุกห้อง |
| เปลี่ยน `440.0` ใน `note_hz` เป็น `442.0` | A4 = 440 Hz |
| ตัดการวนหางออก — เปลี่ยน `mix[(at + i) % frames]` เป็น `if at + i < frames: mix[at + i]` | อบซ้ำได้ไบต์เดิมเป๊ะ **ไม่ล้ม** (ข้อนี้จับไม่ได้ — บันทึกไว้ว่าเป็นข้อที่ต้องใช้หูฟังเท่านั้น) |
| เปลี่ยน `CHANNEL_VOL` ของ lead เป็น `0.01` | ดังพอให้ได้ยิน |
| เปลี่ยน `st.loop_mode` เป็น `LOOP_DISABLED` | สตรีมตั้งลูปไว้แล้ว |

- [ ] **Step 6: commit**

```bash
cd "/Users/artnopparat/IT Dept/Tech part/wealth-quest"
git add godot/audio/music.gd godot/audio/music.gd.uid godot/sim/music_check.gd godot/sim/music_check.gd.uid
git commit -m "Godot: WQMusic — ตารางโน้ตแบบ tracker + ตัวเรียบเรียงเป็น PCM (ยังไม่มีไฟล์เพลง)"
```

---

### Task 2: ตัวอบเพลง + ไฟล์เพลงแรก — **จบขั้นนี้ต้องหยุดให้เจ้าของโปรเจกต์ฟัง**

**Files:**
- Create: `godot/audio/tools/wav_probe.gd`
- Create: `godot/audio/tools/music_bake.gd`
- Create: `godot/audio/music/phase1.wav` · `godot/audio/music/baked.json` (ผลจากตัวอบ)
- Modify: `godot/sim/music_check.gd` — เพิ่ม `_check_files()`
- Modify: `godot/sim/audio_check.gd` — เลิกใช้ `_wav_pcm()` ที่ฝังอยู่ หันไปใช้ `WQWavProbe`

**Interfaces:**
- Consumes: `WQMusic.ids()` · `WQMusic.stream(id)` · `WQMusic.render(id)`
- Produces:
  - `WQWavProbe.pcm(abs_path: String) -> PackedByteArray`
  - `WQWavProbe.peak(pcm: PackedByteArray) -> int`
  - `audio/music/<id>.wav` + `audio/music/baked.json` (คีย์ = id, ค่า = sha256)

- [ ] **Step 1: ย้ายตัวอ่าน PCM ออกมาเป็นของใช้ร่วม — `godot/audio/tools/wav_probe.gd`**

```gdscript
class_name WQWavProbe
extends RefCounted
## อ่านข้อมูลเสียงดิบออกจากไฟล์ .wav บนดิสก์ — **ไม่ใช่ผ่าน load()**
##
## Godot นำเข้า .wav เป็น QOA ที่ถูกบีบอัด `AudioStreamWAV.data` จึงไม่ใช่ PCM
## ถอดเป็น s16 แล้วได้ค่าขยะ (ตรวจแล้ว: ทุกไฟล์ให้พีค ~32767 เท่ากันหมด = เช็กที่ผ่านตลอด
## โดยไม่ได้ตรวจอะไรเลย) และไฟล์ดิบคือสิ่งที่คนทำเสียง/คนทำเพลงจะเอามาวางทับจริงๆ ด้วย

## คืนช่วง PCM ของไฟล์ .wav — ไล่หา chunk "data" จริงๆ ไม่ใช่ข้ามหัว 44 ไบต์ตายตัว
## เพราะไฟล์ที่คนทำเสียงส่งมามักมี chunk เสริม (LIST/INFO ของโปรแกรมตัดต่อ) คั่นอยู่ก่อน
static func pcm(abs_path: String) -> PackedByteArray:
	if not FileAccess.file_exists(abs_path): return PackedByteArray()
	var raw := FileAccess.get_file_as_bytes(abs_path)
	if raw.size() < 12 or raw.slice(0, 4).get_string_from_ascii() != "RIFF":
		return PackedByteArray()
	var pos := 12
	while pos + 8 <= raw.size():
		var id4 := raw.slice(pos, pos + 4).get_string_from_ascii()
		var size := raw.decode_u32(pos + 4)
		var body := pos + 8
		if id4 == "data":
			return raw.slice(body, mini(body + int(size), raw.size()))
		pos = body + int(size) + (int(size) & 1)      # chunk ยาวเลขคี่มีไบต์ padding ต่อท้าย
	return PackedByteArray()


## แอมพลิจูดสูงสุดของช่วง PCM (0–32767)
static func peak(data: PackedByteArray) -> int:
	var out := 0
	var i := 0
	while i + 1 < data.size():
		out = maxi(out, absi(data.decode_s16(i)))
		i += 2
	return out
```

- [ ] **Step 2: ให้ `audio_check` ใช้ตัวกลางแทนของที่ฝังอยู่ข้างใน**

ใน `godot/sim/audio_check.gd` — ลบฟังก์ชัน `_wav_pcm()` ทั้งตัว (อยู่เหนือ `func _eq(`) แล้วแก้บล็อกที่เรียกใช้:

```gdscript
	# เดิม
	var raw := FileAccess.get_file_as_bytes(abs)
	var pcm := _wav_pcm(raw)
	...
	var peak := 0
	var i := 0
	while i + 1 < pcm.size():
		peak = maxi(peak, absi(pcm.decode_s16(i)))
		i += 2

	# ใหม่
	var pcm := WQWavProbe.pcm(abs)
	...
	var peak := WQWavProbe.peak(pcm)
```

- [ ] **Step 3: รัน `audio_check` ให้ยังเขียวเหมือนเดิม**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅` (ถ้าล้ม แปลว่าย้ายโค้ดผิด ไม่ใช่เจอบั๊กใหม่)

- [ ] **Step 4: เขียนเทสต์ไฟล์เพลงที่ต้องล้มก่อน — เพิ่มใน `godot/sim/music_check.gd`**

เพิ่ม `"files"` เข้า `ALL_CHECKS` เรียก `_check_files()` ต่อจาก `_check_tracks()` ใน `_init()` แล้วเพิ่ม:

```gdscript
## ไฟล์เพลงต้องครบ ไม่มีกำพร้า และต้องเป็นของที่ตัวอบทำเองจริงๆ
## กติกาเดียวกับ audio/sfx ทุกข้อ — ต่างแค่โฟลเดอร์
func _check_files() -> void:
	var on_disk := {}
	var d := DirAccess.open(MUSIC_DIR)
	if d == null:
		_fails += 1
		print("  ❌ ไม่มีโฟลเดอร์ %s" % MUSIC_DIR)
		return
	for f in d.get_files():
		if f.ends_with(".wav"): on_disk[f.get_basename()] = true

	var stamp := FileAccess.open("%s/baked.json" % MUSIC_DIR, FileAccess.READ)
	_eq("มี baked.json ของเพลง", stamp != null, true)
	if stamp == null: return
	var marks = JSON.parse_string(stamp.get_as_text())
	stamp.close()
	_eq("baked.json ของเพลงเป็น Dictionary", marks is Dictionary, true)
	if not marks is Dictionary: return

	var seen := {}
	for id in WQMusic.ids():
		var path := "%s/%s.wav" % [MUSIC_DIR, id]
		_eq("มีไฟล์เพลง \"%s\"" % id, ResourceLoader.exists(path), true)
		if not ResourceLoader.exists(path): continue
		on_disk.erase(id)

		var abs := ProjectSettings.globalize_path(path)
		_eq("แฮชที่จดของ \"%s\" ตรงกับไฟล์บนดิสก์จริง" % id,
			String(marks.get(id, "")), FileAccess.get_sha256(abs))

		var pcm := WQWavProbe.pcm(abs)
		_eq("\"%s\" มีข้อมูลเสียงอยู่จริงในไฟล์" % id, pcm.size() > 0, true)
		_eq("\"%s\" ดังพอให้ได้ยิน (พีคเกิน 10%% ของเต็มสเกล)" % id,
			WQWavProbe.peak(pcm) > 3276, true)
		_eq("\"%s\" ยาวตรงกับที่ตารางโน้ตบอก" % id,
			pcm.size() / 2, int(round(WQMusic.length_sec(id) * float(WQSynth.RATE))))

		var key := Marshalls.raw_to_base64(pcm).sha256_text()
		_eq("\"%s\" ไม่ได้เป็นเพลงชุดเดียวกับ \"%s\"" % [id, String(seen.get(key, ""))],
			seen.has(key), false)
		seen[key] = id

	_eq("ไม่มีไฟล์เพลงกำพร้า", on_disk.keys(), [])
	_completed["files"] = true
```

- [ ] **Step 5: รันให้เห็นว่าล้มจริง**

```bash
cd godot && godot --headless --path . --script res://sim/music_check.gd
```

Expected: FAIL — `❌ ไม่มีโฟลเดอร์ res://audio/music` และ `❌ เช็ก "files" ไม่รันจบฟังก์ชัน`

- [ ] **Step 6: เขียนตัวอบ `godot/audio/tools/music_bake.gd`**

```gdscript
extends SceneTree
## อบเพลงใน WQMusic.TRACKS ออกเป็นไฟล์ `.wav` จริง
##   godot --headless --path . --script res://audio/tools/music_bake.gd
##   godot --headless --path . --script res://audio/tools/music_bake.gd -- phase1
##   WQ_MUSIC_FORCE=1 ...      เขียนทับแม้กระทั่งไฟล์ที่คนทำเพลงวางไว้
##
## กติกาตราประทับเหมือน sfx_bake ทุกข้อ: จด sha256 ของไฟล์ที่อบเองไว้ใน baked.json
## แฮชบนดิสก์ไม่ตรงกับที่จด = มีคนวางไฟล์จริงทับ **ตัวอบจะไม่แตะไฟล์นั้นเด็ดขาด**
## และเช็ก "แฮชตรงกับที่เรนเดอร์ตอนนี้เป๊ะไหม" ก่อนเช็กตราเสมอ เพื่อไม่ให้ไฟล์ของตัวอบเอง
## ที่ตราหายไป (โดนขัดจังหวะก่อนบันทึก baked.json) ถูกเข้าใจผิดว่าเป็นของคนทำเพลง
##
## เพลงหนึ่งเพลงยาวหลายสิบวินาที การเรนเดอร์ใช้เวลาหลายวินาทีต่อเพลง — ปกติ ไม่ได้ค้าง

const MUSIC_DIR := "res://audio/music"
const STAMP := "res://audio/music/baked.json"
const TMP := "user://music_bake_tmp.wav"

var _written := 0
var _skipped_human := 0
var _skipped_same := 0
var _fails := 0


func _init() -> void:
	var only: Array = []
	for a in OS.get_cmdline_user_args(): only.append(String(a))
	var force := OS.get_environment("WQ_MUSIC_FORCE") != ""
	print("=== music_bake%s ===" % ("" if only.is_empty() else " (เฉพาะ %d เพลง)" % only.size()))

	DirAccess.make_dir_recursive_absolute(ProjectSettings.globalize_path(MUSIC_DIR))
	var marks := _load_marks()

	for id in WQMusic.ids():
		if not only.is_empty() and not only.has(id): continue
		_bake(String(id), marks, force)

	# ลบตราของเพลงที่หลุดออกจาก TRACKS ไปแล้ว — ทำเฉพาะรอบที่อบครบทุกเพลงเท่านั้น
	# รอบที่กรองด้วย -- id เห็นแค่บางส่วน ถ้าลบตรงนี้ ตราของเพลงที่ไม่ได้แตะจะหายหมด
	if only.is_empty():
		for k in marks.keys().duplicate():
			if not WQMusic.ids().has(k): marks.erase(k)

	_save_marks(marks)
	print("music_bake: เขียน %d · ข้ามของที่คนทำ %d · ไม่เปลี่ยน %d%s" % [
		_written, _skipped_human, _skipped_same,
		"" if _fails == 0 else " · ล้มเหลว %d ❌" % _fails])
	if _written > 0:
		print("  อย่าลืม: godot --headless --path . --import   แล้วรัน music_check")
	quit(1 if _fails > 0 else 0)


func _bake(id: String, marks: Dictionary, force: bool) -> void:
	var path := "%s/%s.wav" % [MUSIC_DIR, id]
	var abs := ProjectSettings.globalize_path(path)
	var tmp_abs := ProjectSettings.globalize_path(TMP)

	var err := WQMusic.stream(id).save_to_wav(TMP)
	if err != OK:
		_fails += 1
		print("  ❌ %s — เขียนไฟล์ที่พักไม่ได้ (error %d)" % [id, err])
		DirAccess.remove_absolute(tmp_abs)
		return
	var fresh := FileAccess.get_sha256(tmp_abs)

	if FileAccess.file_exists(abs):
		var have := FileAccess.get_sha256(abs)
		if have == fresh:
			if String(marks.get(id, "")) != fresh:
				marks[id] = fresh
				_save_marks(marks)
			_skipped_same += 1
			DirAccess.remove_absolute(tmp_abs)
			return
		if have != String(marks.get(id, "")) and not force:
			_skipped_human += 1
			print("  🙌 %s — มีเพลงที่คนทำอยู่แล้ว ไม่แตะ" % id)
			DirAccess.remove_absolute(tmp_abs)
			return

	if DirAccess.copy_absolute(tmp_abs, abs) != OK:
		_fails += 1
		print("  ❌ %s — ก๊อปจากที่พักไม่ได้" % id)
		DirAccess.remove_absolute(tmp_abs)
		return
	DirAccess.remove_absolute(tmp_abs)
	marks[id] = fresh
	_save_marks(marks)
	_written += 1
	print("  ✅ %s -> %s (%.1f วิ)" % [id, path, WQMusic.length_sec(id)])


func _load_marks() -> Dictionary:
	var f := FileAccess.open(STAMP, FileAccess.READ)
	if f == null: return {}
	var parsed = JSON.parse_string(f.get_as_text())
	f.close()
	return parsed if parsed is Dictionary else {}


func _save_marks(marks: Dictionary) -> void:
	var f := FileAccess.open(STAMP, FileAccess.WRITE)
	if f == null:
		_fails += 1
		print("  ❌ เขียน baked.json ของเพลงไม่ได้")
		return
	f.store_string(JSON.stringify(marks, "\t", true))
	f.close()
```

- [ ] **Step 7: อบเพลงแรกแล้วรันเทสต์ให้ผ่าน**

```bash
cd godot
godot --headless --path . --script res://audio/tools/music_bake.gd
godot --headless --path . --import
godot --headless --path . --script res://sim/music_check.gd
ls -la audio/music/
```

Expected: `music_bake: เขียน 1 …` · `music_check: ผ่านทั้งหมด ✅` · ไฟล์ `phase1.wav` ราว 1.5 MB

- [ ] **Step 8: พิสูจน์ว่าตราประทับกันทับงานคนจริง**

```bash
cd godot
cp audio/music/phase1.wav /tmp/mine.wav
printf 'x' >> audio/music/phase1.wav     # แกล้งเป็นไฟล์ที่คนทำเพลงวางทับ
godot --headless --path . --script res://audio/tools/music_bake.gd
```

Expected: `🙌 phase1 — มีเพลงที่คนทำอยู่แล้ว ไม่แตะ` แล้วคืนของเดิม:

```bash
cp /tmp/mine.wav audio/music/phase1.wav && godot --headless --path . --script res://sim/music_check.gd
```

Expected: `music_check: ผ่านทั้งหมด ✅`

- [ ] **Step 9: commit**

```bash
cd "/Users/artnopparat/IT Dept/Tech part/wealth-quest"
git add godot/audio/tools godot/audio/music godot/sim/music_check.gd godot/sim/audio_check.gd
git commit -m "Godot: ตัวอบเพลง + เพลงด่าน 1 ไฟล์แรก + WQWavProbe ใช้ร่วมสองสูท"
```

- [ ] **Step 10: 🛑 หยุดให้เจ้าของโปรเจกต์ฟังก่อนไปต่อ**

ยังเล่นในเกมไม่ได้ (Task 3 ยังไม่ทำ) ให้ฟังจากไฟล์ตรงๆ:

```bash
afplay "godot/audio/music/phase1.wav"      # macOS
```

รายงานว่า "อบเพลงด่าน 1 แล้ว ฟังแล้วบอกได้เลยว่าจะเอาแบบไหน" **ห้ามเริ่ม Task 3 จนกว่าจะได้คำตอบ**
ถ้าต้องแก้: แก้สตริงใน `WQMusic.TRACKS` → `music_bake -- phase1` → `--import` → ฟังใหม่

---

### Task 3: เลนเพลงใน `WQAudio` — บัส Music · เฟดไขว้ · สไลเดอร์

**Files:**
- Modify: `godot/audio/audio.gd`
- Modify: `godot/ui/widgets/audio_panel.gd`
- Modify: `godot/sim/music_check.gd` — เพิ่ม `_check_lane()`

**Interfaces:**
- Consumes: `WQMusic.TRACKS` · `WQMusic.stream(id)` · `WQMusic.ids()`
- Produces:
  - `WQAudio.play_music(id: String) -> void` · `WQAudio.stop_music() -> void`
  - `WQAudio.music_now: String` — id ที่ควรกำลังดัง ("" = ไม่มี)
  - `WQAudio.FADE: float` = 0.8 · `WQAudio._music: Array[AudioStreamPlayer]` (สองตัว)
  - `_levels` มีคีย์ `"Music"` · `settings.cfg` มีคีย์ `music`
  - `WQAudioPanel._music: HSlider`

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มก่อน — เพิ่มใน `godot/sim/music_check.gd`**

เพิ่ม `"lane"` เข้า `ALL_CHECKS` · `_init()` ต้องรอเฟรมก่อนแตะ WQAudio (โหนดต้องเข้า tree ก่อน):

```gdscript
func _init() -> void:
	_check_tracks()
	_check_files()
	await process_frame
	await _check_lane()
	for name in ALL_CHECKS:
		...
```

```gdscript
## เพลงต้องเปลี่ยนด้วยการเฟดไขว้ ไม่ใช่ตัดทันที และเพลงเดิมต้องไม่เริ่มใหม่
## `changed` ยิงหลายสิบครั้งต่อเดือน ถ้าไม่จำว่าเพลงไหนดังอยู่ เพลงจะเริ่มใหม่ตลอดเวลา
func _check_lane() -> void:
	WQAudio.settings_path = "user://settings_music_test.cfg"
	DirAccess.remove_absolute(ProjectSettings.globalize_path(WQAudio.settings_path))
	WQAudio.set_muted(false)
	WQAudio.set_level("Music", 1.0)

	_eq("มีบัส Music", AudioServer.get_bus_index("Music") >= 0, true)
	_eq("มีตัวเล่นเพลงสองตัว", WQAudio._music.size(), 2)
	_eq("ตัวเล่นเพลงอยู่บนบัส Music", WQAudio._music[0].bus, "Music")

	WQAudio.stop_music()
	_eq("เริ่มมายังไม่มีเพลง", WQAudio.music_now, "")

	WQAudio.play_music("phase1")
	_eq("สั่งเล่นแล้วจำว่าเพลงไหนดังอยู่", WQAudio.music_now, "phase1")

	## เพลงเดิมสั่งซ้ำต้องไม่ทำอะไรเลย — นับที่ music_switches ไม่ใช่ที่ตัวเล่นที่เป็นตัวหลัก
	## เพราะ headless เข้า _silent แล้วคืนก่อนจะสลับตัวเล่นอยู่แล้ว เทสต์ที่ดูตัวเล่นจึงเขียวเสมอ
	## ไม่ว่าโค้ดจะกันการสั่งซ้ำจริงหรือไม่ (เขียวปลอมแบบเดียวกับที่ audio_check เคยโดนมาแล้ว)
	var n := WQAudio.music_switches
	WQAudio.play_music("phase1")
	_eq("สั่งเพลงเดิมซ้ำต้องไม่นับเป็นการเปลี่ยนเพลง", WQAudio.music_switches, n)

	# id ที่ไม่มีในตารางต้องไม่เปลี่ยนอะไร
	WQAudio.play_music("ไม่มีเพลงนี้")
	_eq("สั่งเพลงที่ไม่มีต้องไม่เปลี่ยนเพลงที่ดังอยู่", WQAudio.music_now, "phase1")

	WQAudio.stop_music()
	_eq("สั่งหยุดแล้วไม่มีเพลง", WQAudio.music_now, "")

	# ปิดเสียงทั้งหมดต้องครอบเพลงด้วย ไม่ใช่เงียบแค่เสียงสั้น
	WQAudio.set_muted(true)
	_eq("ปิดเสียงแล้วบัส Music ถูกปิดด้วย",
		AudioServer.is_bus_mute(AudioServer.get_bus_index("Music")), true)
	WQAudio.set_muted(false)
	_eq("เปิดเสียงกลับแล้วบัส Music กลับมาดัง",
		AudioServer.is_bus_mute(AudioServer.get_bus_index("Music")), false)

	# สตรีมของเพลงต้องตั้งลูปไว้ — ไฟล์ที่ import มาได้ loop_mode = 0 เสมอ
	var st := WQAudio._music_stream("phase1") as AudioStreamWAV
	_eq("สตรีมเพลงตั้งลูปไว้", st.loop_mode, AudioStreamWAV.LOOP_FORWARD)
	_eq("จุดจบลูปไม่ใช่ศูนย์", st.loop_end > 0, true)

	# ระดับเสียงเพลงต้องลงบัสจริงและถูกจำลงไฟล์ตั้งค่า
	WQAudio.set_level("Music", 0.4)
	_eq("ระดับเสียงเพลงลงบัสจริง",
		snappedf(AudioServer.get_bus_volume_db(AudioServer.get_bus_index("Music")), 0.01),
		snappedf(linear_to_db(0.4), 0.01))
	var cfg := ConfigFile.new()
	_eq("เขียนไฟล์ตั้งค่าแล้ว", cfg.load(WQAudio.settings_path), OK)
	_eq("จำระดับเสียงเพลง", float(cfg.get_value("audio", "music", -1.0)), 0.4)
	WQAudio._levels["Music"] = 1.0
	WQAudio._load_settings()
	_eq("โหลดระดับเสียงเพลงกลับมาได้", WQAudio.get_level("Music"), 0.4)
	WQAudio.set_level("Music", 1.0)

	# แผงปรับเสียงต้องมีสไลเดอร์เพลง และขยับแล้วมีผลจริง
	var panel := WQAudioPanel.new()
	root.add_child(panel)
	await process_frame
	_eq("สไลเดอร์เพลงตั้งค่าตามที่จำไว้", panel._music.value, 1.0)
	panel._music.value = 0.5
	await process_frame
	_eq("ขยับสไลเดอร์เพลงแล้วระดับเสียงเปลี่ยนจริง", WQAudio.get_level("Music"), 0.5)
	WQAudio.set_level("Music", 1.0)
	root.remove_child(panel)
	panel.free()

	_completed["lane"] = true
```

- [ ] **Step 2: รันให้เห็นว่าล้มจริง**

```bash
cd godot && godot --headless --path . --script res://sim/music_check.gd
```

Expected: FAIL — `Cannot find member "_music" in base "WQAudio"`

- [ ] **Step 3: เพิ่มเลนเพลงใน `godot/audio/audio.gd`**

ค่าคงที่ (ต่อจาก `const HISTORY`):

```gdscript
const MUSIC_DIR := "res://audio/music"
const FADE := 0.8                ## วินาทีที่ใช้เฟดไขว้ตอนสลับเพลง
```

สถานะ (ต่อจาก `static var _was_critical`):

```gdscript
static var music_now := ""       ## id ของเพลงที่ควรกำลังดัง — "" = ไม่มี (เทสต์อ่านตัวนี้)
static var music_switches := 0   ## นับจำนวนครั้งที่ "เปลี่ยนเพลงจริง" — เทสต์ใช้จับการสั่งซ้ำ
static var _music: Array[AudioStreamPlayer] = []
static var _music_at := 0        ## ตัวเล่นที่เป็นตัวหลักตอนนี้
static var _music_cache := {}
static var _music_tween: Tween
```

`_levels` เพิ่มคีย์ Music:

```gdscript
static var _levels := {"Master": 1.0, "SFX": 1.0, "Music": 1.0}
```

`_ready()` — หลังลูปสร้างตัวเล่น sfx ให้เพิ่ม:

```gdscript
	for _i in 2:
		var mp := AudioStreamPlayer.new()
		mp.bus = "Music"
		add_child(mp)
		_music.append(mp)
```

`_ensure_bus()` — รับชื่อบัสเป็นพารามิเตอร์แทนที่จะฝัง "SFX" ไว้:

```gdscript
## headless เริ่มมาด้วยบัสเดียว (ตรวจแล้ว: bus_count = 1) จึงสร้างบัสเองแทนการ
## พึ่ง default_bus_layout.tres — ได้ผลเหมือนกันทุกโหมดและเทสต์ headless ได้จริง
static func _ensure_bus(name: String) -> void:
	if AudioServer.get_bus_index(name) >= 0: return
	var idx := AudioServer.bus_count
	AudioServer.add_bus(idx)
	AudioServer.set_bus_name(idx, name)
	AudioServer.set_bus_send(idx, "Master")
```

แล้วใน `_ready()` เปลี่ยน `_ensure_bus()` เป็นสองบรรทัด **ก่อน** ลูปสร้างตัวเล่นทั้งสองชุด:

```gdscript
	_ensure_bus("SFX")
	_ensure_bus("Music")
```

`_load_settings()` / `_save_settings()` เพิ่มคีย์ `music`:

```gdscript
		_levels["Music"] = float(cfg.get_value("audio", "music", 1.0))
```
```gdscript
	cfg.set_value("audio", "music", _levels["Music"])
```

เลนเพลง (วางต่อจากบล็อก `# ========== เลน UI ==========`):

```gdscript
# ========== เลนเพลง ==========

## เปลี่ยนเพลงพื้นหลังด้วยการเฟดไขว้ · **สั่งเพลงที่ดังอยู่แล้วต้องไม่ทำอะไรเลย**
## `_refresh_music()` ถูกเรียกทุกครั้งที่สถานะเปลี่ยน ซึ่งหลายสิบครั้งต่อเดือน
## ถ้าไม่กันตรงนี้ เพลงจะเริ่มใหม่ตลอดเวลาจนฟังไม่ได้เลย
static func play_music(id: String) -> void:
	if id == music_now: return
	if not WQMusic.TRACKS.has(id): return
	music_now = id
	music_switches += 1
	if _silent or _music.size() < 2: return

	var from := _music[_music_at]
	_music_at = (_music_at + 1) % _music.size()
	var to := _music[_music_at]
	to.stream = _music_stream(id)
	to.volume_db = linear_to_db(0.0001)
	to.play()

	if _music_tween != null and _music_tween.is_valid(): _music_tween.kill()
	_music_tween = inst.create_tween()
	_music_tween.set_parallel(true)
	_music_tween.tween_property(to, "volume_db", 0.0, FADE)
	_music_tween.tween_property(from, "volume_db", linear_to_db(0.0001), FADE)
	_music_tween.chain().tween_callback(from.stop)


static func stop_music() -> void:
	music_now = ""
	if _music_tween != null and _music_tween.is_valid(): _music_tween.kill()
	for p in _music:
		p.stop()


## ไฟล์เพลงที่ import มาได้ `loop_mode = 0` เสมอ (ตรวจแล้วกับ Godot 4.7.1) จุดลูปจึงต้อง
## ตั้งจากโค้ด และต้อง `duplicate()` ก่อน ไม่งั้นไปแก้ตัวที่ทั้งโปรเจกต์แคชร่วมกันอยู่
## ถ้าไฟล์หาย ตกกลับไปเรียบเรียงสดให้เกมยังมีเพลง (เพิ่ง clone แล้วยังไม่ได้ --import)
static func _music_stream(id: String) -> AudioStream:
	if _music_cache.has(id): return _music_cache[id]
	var path := "%s/%s.wav" % [MUSIC_DIR, id]
	var st: AudioStream
	if ResourceLoader.exists(path):
		var w: AudioStreamWAV = (load(path) as AudioStreamWAV).duplicate()
		w.loop_mode = AudioStreamWAV.LOOP_FORWARD
		w.loop_begin = 0
		w.loop_end = int(round(w.get_length() * float(w.mix_rate)))
		st = w
	else:
		st = WQMusic.stream(id)
	_music_cache[id] = st
	return st
```

- [ ] **Step 4: เพิ่มสไลเดอร์เพลงใน `godot/ui/widgets/audio_panel.gd`**

เพิ่มสมาชิก `var _music: HSlider` ใต้ `var _sfx: HSlider` แล้วเพิ่มต่อจากสไลเดอร์ `_sfx`:

```gdscript
	_music = _slider(col, "เพลง", WQAudio.get_level("Music"))
	_music.value_changed.connect(func(v: float): WQAudio.set_level("Music", v))
```

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/music_check.gd
```

Expected: `music_check: ผ่านทั้งหมด ✅`

- [ ] **Step 6: รันสูททั้งชุดให้ยังเขียวหมด**

```bash
cd godot
for s in rng_check headless_sim save_check ui_check flow_check world_check audio_check music_check; do
  godot --headless --path . --script res://sim/$s.gd >/dev/null 2>&1 || echo "FAILED: $s"
done
```

Expected: ไม่มีบรรทัด `FAILED:` (`audio_check` ต้องยังผ่าน — บัส Music ตัวใหม่ต้องไม่กระทบเสียงสั้น)

- [ ] **Step 7: พิสูจน์ว่าเทสต์จับของจริง**

| ทำลาย | เทสต์ที่ต้องล้ม |
|---|---|
| ลบบรรทัด `if id == music_now: return` | สั่งเพลงเดิมซ้ำต้องไม่นับเป็นการเปลี่ยนเพลง |
| ลบบรรทัด `_ensure_bus("Music")` | มีบัส Music |
| ลบสามบรรทัดที่ตั้ง `loop_mode`/`loop_begin`/`loop_end` ใน `_music_stream` | สตรีมเพลงตั้งลูปไว้ |
| ลบบรรทัด `cfg.set_value("audio", "music", ...)` | จำระดับเสียงเพลง |
| ลบ `AudioServer.set_bus_mute(...)` ออกจาก `_apply_levels()` | ปิดเสียงแล้วบัส Music ถูกปิดด้วย |
| ลบบรรทัด `_music.value_changed.connect(...)` ในแผง | ขยับสไลเดอร์เพลงแล้วระดับเสียงเปลี่ยนจริง |

คืนโค้ดเดิมทุกครั้งหลังทำลาย แล้วรันสูทยืนยันว่ากลับมาเขียว

- [ ] **Step 8: commit**

```bash
cd "/Users/artnopparat/IT Dept/Tech part/wealth-quest"
git add godot/audio/audio.gd godot/ui/widgets/audio_panel.gd godot/sim/music_check.gd
git commit -m "Godot: เลนเพลงใน WQAudio — บัส Music เฟดไขว้ 0.8 วิ + สไลเดอร์เพลงในแผง"
```

---

### Task 4: เพลงที่สองและสาม + นโยบายเลือกเพลงจากสถานะ

**Files:**
- Modify: `godot/audio/music.gd` — เพิ่มเพลง `phase2` · `crisis`
- Modify: `godot/audio/audio.gd`
- Modify: `godot/sim/music_check.gd` — เพิ่ม `_check_policy()`
- Create: `godot/audio/music/phase2.wav` · `godot/audio/music/crisis.wav` (ผลจากตัวอบ)

**Interfaces:**
- Consumes: `WQAudio.bind()` · `WQAudio.bind_player()` · `WQPlayer.health` · `WQPlayer.phase` ·
  `WQMatch.active_disasters`
- Produces: `WQAudio.CRISIS_HEALTH: float` = 40.0 · `WQAudio._want_music() -> String` ·
  `WQAudio._refresh_music() -> void`

- [ ] **Step 1: แต่งเพลงด่าน 2 และเพลงวิกฤตเพิ่มใน `WQMusic.TRACKS`**

ต้องมีคีย์ทั้งสามอยู่จริงก่อน ไม่งั้นเทสต์นโยบายพิสูจน์อะไรไม่ได้ — `play_music()` ปฏิเสธ id
ที่ไม่มีในตาราง เพลงจึงค้างที่ `phase1` แล้วเทสต์จะล้มด้วยเหตุผลที่ไม่เกี่ยวกับนโยบายเลย
โครงเดียวกับ `phase1` (16 ห้อง · 4 จังหวะ/ห้อง) ต่างที่อารมณ์ · **แต่งหลังจากได้ฟังเสียงตอบรับ
ของ `phase1` จาก Task 2 แล้วเท่านั้น** ถ้าเจ้าของสั่งแก้แนวเพลง ให้แก้ `phase1` ก่อนแล้วค่อยแต่งสองตัวนี้
ตามแนวที่ตกลงกันใหม่:

โครงเดียวกับ `phase1` (16 ห้อง · 4 จังหวะ/ห้อง) ต่างที่อารมณ์:

```gdscript
	## ด่าน 2 — ออกจากสนามแข่งหนูแล้ว กำลังไล่ตามความฝัน: สว่างขึ้น เร็วขึ้นเล็กน้อย
	## ขยับไป G major (คอร์ด G–Em–C–D) ให้ต่างจากด่าน 1 แบบที่หูจับได้ทันทีโดยไม่ต้องบอก
	"phase2": {
		"bpm": 124, "beats_per_bar": 4,
		"lead":
			"G4 .  B4 D5 | E5 .  D5 B4 | C5 .  E5 .  | D5 -  -  .  " +
			"G4 .  B4 D5 | E5 .  G5 .  | D5 .  C5 B4 | G4 -  -  .  " +
			"E5 .  D5 .  | C5 .  B4 G4 | A4 .  C5 E5 | D5 -  -  .  " +
			"G5 .  E5 .  | D5 .  B4 .  | C5 .  D5 .  | G4 -  -  . ",
		"bass":
			"G2 -  -  -  | E2 -  -  -  | C2 -  -  -  | D2 -  -  -  " +
			"G2 -  -  -  | E2 -  -  -  | C2 -  -  -  | D2 -  -  -  " +
			"G2 -  -  -  | E2 -  -  -  | C2 -  -  -  | D2 -  -  -  " +
			"G2 -  -  -  | E2 -  -  -  | C2 -  -  -  | D2 -  -  - ",
		"drum":
			"X  .  x  x  | X  .  x  .  | X  .  x  x  | X  x  x  X  " +
			"X  .  x  x  | X  .  x  .  | X  .  x  x  | X  x  x  X  " +
			"X  .  x  x  | X  .  x  .  | X  .  x  x  | X  x  x  X  " +
			"X  .  x  x  | X  .  x  .  | X  .  x  x  | X  x  x  X ",
	},

	## วิกฤต — สุขภาพจะหมดหรือภัยพิบัติลงแล้ว: A minor ช้าลง ทำนองวนแคบๆ ไม่ไปไหน
	## ตั้งใจให้ "อึดอัด" ไม่ใช่ "น่ากลัว" เพราะมันดังค้างได้หลายเดือนติดกัน
	## เสียงที่น่ากลัวจริงๆ ฟังนานๆ แล้วผู้เล่นจะปิดเสียงเกมทิ้ง ซึ่งแย่กว่าไม่มีเพลงเลย
	"crisis": {
		"bpm": 84, "beats_per_bar": 4,
		"lead":
			"A4 .  .  C5 | B4 .  A4 .  | G4 .  .  A4 | E4 -  -  .  " +
			"A4 .  .  C5 | B4 .  G4 .  | F4 .  E4 .  | E4 -  -  .  " +
			"C5 .  B4 .  | A4 .  .  G4 | F4 .  .  E4 | D4 -  -  .  " +
			"A4 .  .  .  | G4 .  .  .  | F4 .  E4 .  | A4 -  -  . ",
		"bass":
			"A1 -  -  -  | A1 -  -  -  | F1 -  -  -  | E1 -  -  -  " +
			"A1 -  -  -  | A1 -  -  -  | F1 -  -  -  | E1 -  -  -  " +
			"A1 -  -  -  | A1 -  -  -  | D2 -  -  -  | E1 -  -  -  " +
			"A1 -  -  -  | A1 -  -  -  | F1 -  -  -  | E1 -  -  - ",
		"drum":
			"X  .  .  .  | .  .  x  .  | X  .  .  .  | .  .  x  .  " +
			"X  .  .  .  | .  .  x  .  | X  .  .  .  | .  .  x  .  " +
			"X  .  .  .  | .  .  x  .  | X  .  .  .  | .  .  x  .  " +
			"X  .  .  .  | .  .  x  .  | X  .  .  .  | .  .  x  . ",
	},
```

- [ ] **Step 2: เขียนเทสต์ที่ต้องล้มก่อน — เพิ่มใน `godot/sim/music_check.gd`**

เพิ่ม `"policy"` เข้า `ALL_CHECKS` เรียก `_check_policy()` ต่อจาก `_check_lane()` แล้วเพิ่ม:

```gdscript
## เพลงต้องเปลี่ยนตามสถานะจริงของผู้เล่นที่ผูกไว้ ไม่ใช่ให้ ui/ สั่ง (กฎเหล็กข้อ 6)
## และวิกฤตต้องชนะทุกเงื่อนไข — ตอนสุขภาพจะหมดหรือภัยพิบัติลงแล้วยังได้ยินเพลงเดินเรื่อยๆ
## คือเกมโกหกผู้เล่นว่าทุกอย่างปกติดี
func _check_policy() -> void:
	WQData.load_all()
	var m := WQMatch.new()
	m.setup({"mode": "solo", "seed": 20260821, "players": [
		{"name": "คุณ", "job_id": "teacher", "is_ai": false},
	]})
	var me = m.players[0]

	WQAudio.bind(m)
	WQAudio.bind_player(me)

	me.health = 70.0
	me.phase = 1
	m.active_disasters = []
	me.changed.emit()
	_eq("ด่าน 1 ปกติ ได้เพลงด่าน 1", WQAudio.music_now, "phase1")

	me.phase = 2
	me.changed.emit()
	_eq("เข้าด่าน 2 แล้วเปลี่ยนเพลง", WQAudio.music_now, "phase2")

	me.health = 30.0
	me.changed.emit()
	_eq("สุขภาพต่ำกว่าเกณฑ์ วิกฤตชนะเงื่อนไขด่าน", WQAudio.music_now, "crisis")

	me.health = 70.0
	me.changed.emit()
	_eq("สุขภาพกลับมา ได้เพลงด่าน 2 คืน", WQAudio.music_now, "phase2")

	m.active_disasters = [{"def": {}, "left": 2}]
	m.month_ended.emit(3)
	_eq("มีภัยพิบัติค้างอยู่ ได้เพลงวิกฤต", WQAudio.music_now, "crisis")

	m.active_disasters = []
	m.month_ended.emit(4)
	_eq("ภัยพิบัติหมดอายุแล้ว กลับไปเพลงด่าน", WQAudio.music_now, "phase2")

	# ยังไม่มีผู้เล่นผูกไว้ (หน้าเลือกอาชีพ) ต้องเป็นเพลงด่าน 1
	WQAudio.bind_player(null)
	WQAudio.stop_music()
	WQAudio._refresh_music()
	_eq("ยังไม่มีแมตช์ ได้เพลงด่าน 1", WQAudio.music_now, "phase1")

	_completed["policy"] = true
```

- [ ] **Step 3: รันให้เห็นว่าล้มจริง**

```bash
cd godot && godot --headless --path . --script res://sim/music_check.gd
```

Expected: FAIL — `❌ เข้าด่าน 2 แล้วเปลี่ยนเพลง: ได้ phase1 ต้องการ phase2` (ยังไม่มีนโยบาย)

- [ ] **Step 4: เพิ่มนโยบายใน `godot/audio/audio.gd`**

ค่าคงที่:

```gdscript
const CRISIS_HEALTH := 40.0      ## เกณฑ์เดียวกับเสียงเตือน health_low
```

ต่อท้ายบล็อกเลนเพลง:

```gdscript
## เลือกเพลงจากสถานะที่ผูกไว้ — **นี่คือที่เดียวที่ตัดสินว่าเพลงไหนควรดัง**
## `ui/` สั่งไม่ได้ (กฎเหล็กข้อ 6) เพราะถ้าสั่งได้ วันหนึ่งจะมีหน้าจอที่ลืมสั่งเปลี่ยนกลับ
## แล้วเพลงวิกฤตจะดังค้างทั้งเกมโดยไม่มีใครรู้ว่าใครเป็นคนสั่ง
static func _want_music() -> String:
	if _player == null: return "phase1"
	if float(_player.health) < CRISIS_HEALTH: return "crisis"
	if _match != null and not _match.active_disasters.is_empty(): return "crisis"
	return "phase2" if int(_player.phase) >= 2 else "phase1"


static func _refresh_music() -> void:
	play_music(_want_music())
```

เรียกจากจุดที่สถานะเปลี่ยน — เพิ่มบรรทัด `_refresh_music()` ท้ายเมธอดเหล่านี้:

```gdscript
func _on_changed() -> void:
	if _player == null: return
	var crit: bool = float(_player.health) < CRISIS_HEALTH
	if crit and not _was_critical: _play("health_low")
	_was_critical = crit
	_refresh_music()          # ← เพิ่ม


func _on_month_ended(_month: int) -> void:
	_play("month_end")
	if _player == null: return
	if _player.get_total_income() - _player.get_total_expenses() > 0.0: _play("payday")
	_refresh_music()          # ← เพิ่ม


func _on_disaster(_def: Dictionary) -> void:
	_play("disaster")
	_refresh_music()          # ← เพิ่ม
```

และท้าย `bind_player()` (หลังบล็อก `if _player != null:` ที่ต่อสายเสร็จ) เพิ่ม `_refresh_music()`

ท้าย `_ready()` (หลัง `_load_settings()`) เพิ่ม `_refresh_music()` ด้วย — **ไม่งั้นเกมเงียบสนิท
จนกว่าจะเริ่มแมตช์** เพราะไม่มีใครเรียกนโยบายเลยตอนอยู่หน้าเลือกอาชีพ (สเปกข้อ "เพลงเริ่มเล่น
ตั้งแต่หน้าเลือกอาชีพ" อยู่ตรงนี้ที่เดียว)

> หมายเหตุ: `_on_changed()` เดิมใช้ `40.0` เขียนตรงๆ — เปลี่ยนเป็น `CRISIS_HEALTH` ให้เกณฑ์
> อยู่ที่เดียว ไม่งั้นวันหนึ่งเสียงเตือนกับเพลงวิกฤตจะใช้เกณฑ์คนละตัว

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --script res://sim/music_check.gd
```

Expected: `music_check: ผ่านทั้งหมด ✅`

`phase2` กับ `crisis` ยังไม่มี **ไฟล์** (อบใน Task 5) — `_check_files()` จะล้มที่ "มีไฟล์เพลง"
ทั้งสองตัว ให้อบทันทีในสเต็ปถัดไปแทนที่จะปล่อยสูทแดงค้างไว้:

```bash
godot --headless --path . --script res://audio/tools/music_bake.gd
godot --headless --path . --import
godot --headless --path . --script res://sim/music_check.gd
```

- [ ] **Step 6: พิสูจน์ว่าเทสต์จับของจริง**

| ทำลาย | เทสต์ที่ต้องล้ม |
|---|---|
| สลับสองบรรทัด: เช็ก `phase >= 2` ก่อนเช็กสุขภาพ | สุขภาพต่ำกว่าเกณฑ์ วิกฤตชนะเงื่อนไขด่าน |
| ลบ `_refresh_music()` ออกจาก `_on_changed()` | เข้าด่าน 2 แล้วเปลี่ยนเพลง |
| ลบ `_refresh_music()` ออกจาก `_on_month_ended()` | มีภัยพิบัติค้างอยู่ ได้เพลงวิกฤต |
| เปลี่ยน `if _player == null: return "phase1"` เป็น `return "crisis"` | ยังไม่มีแมตช์ ได้เพลงด่าน 1 |
| ลบ `_refresh_music()` ออกจากท้าย `_ready()` แล้วรัน `WQ_JOB= godot --path .` | ไม่มีเทสต์จับ — **ต้องฟังเอง** ว่าหน้าเลือกอาชีพยังมีเพลง (บันทึกไว้ว่าข้อนี้เทสต์ไม่ครอบ) |

- [ ] **Step 7: commit**

```bash
cd "/Users/artnopparat/IT Dept/Tech part/wealth-quest"
git add godot/audio/audio.gd godot/audio/music.gd godot/sim/music_check.gd
git commit -m "Godot: นโยบายเลือกเพลง — ด่าน 1/ด่าน 2/วิกฤต ตัดสินจากสถานะที่ผูกไว้"
```

---

### Task 5: โหมดฟังเพลง + เอกสาร + ฟังจริงในเกม

**Files:**
- Modify: `godot/ui/main.gd` — โหมดฟังเพลงจาก terminal
- Modify: `godot/audio/README.md` (ผลจากสคริปต์) · `godot/audio/tools/sfx_readme.gd`
- Modify: `godot/CLAUDE.md` · `godot/README.md`

**Interfaces:**
- Consumes: ทุกอย่างจาก Task 1–4
- Produces: โหมด `WQ_MUSIC=<id>` · `audio/README.md` ที่มีตารางเพลง · เอกสารที่อัปเดตแล้ว

- [ ] **Step 1: ยืนยันว่าไฟล์เพลงครบสามตัวแล้ว**

```bash
cd godot
godot --headless --path . --script res://audio/tools/music_bake.gd
godot --headless --path . --import
godot --headless --path . --script res://sim/music_check.gd
du -sh audio/music
```

Expected: `music_check: ผ่านทั้งหมด ✅` · โฟลเดอร์เพลงราว 4 MB

- [ ] **Step 2: เพิ่มโหมดฟังเพลงจาก terminal ใน `godot/ui/main.gd`**

ต่อจากบล็อก `WQ_SFX` ใน `_ready()`:

```gdscript
	# ฟังเพลงทีละเพลง: WQ_MUSIC=crisis godot --path .   (ปิดเองใน 20 วินาที)
	# มีเพราะ music_check ตรวจได้แค่ว่ามีเสียงและโครงถูก ไม่ได้ตรวจว่าฟังแล้วรู้เรื่อง
	var track := OS.get_environment("WQ_MUSIC")
	if track != "":
		WQAudio.play_music(track)
		await get_tree().create_timer(20.0).timeout
		get_tree().quit()
```

- [ ] **Step 3: ให้ `audio/README.md` มีตารางเพลงด้วย**

ใน `godot/audio/tools/sfx_readme.gd` เพิ่มก่อนบล็อก `## สเปกสำหรับคนทำเสียง`:

```gdscript
	lines.append("")
	lines.append("## เพลงพื้นหลัง")
	lines.append("")
	lines.append("| id | ดังตอนไหน | bpm | ยาว (วิ) | ที่มา |")
	lines.append("|---|---|---|---|---|")
	var mmarks := {}
	var mf := FileAccess.open("res://audio/music/baked.json", FileAccess.READ)
	if mf != null:
		var mparsed = JSON.parse_string(mf.get_as_text())
		if mparsed is Dictionary: mmarks = mparsed
		mf.close()
	const WHEN := {
		"phase1": "ด่าน 1 หรือยังไม่เริ่มแมตช์",
		"phase2": "ด่าน 2",
		"crisis": "สุขภาพต่ำกว่า 40 หรือมีภัยพิบัติค้างอยู่",
	}
	for id in WQMusic.ids():
		var mabs := ProjectSettings.globalize_path("res://audio/music/%s.wav" % id)
		var msrc := "❓ ยังไม่มีไฟล์"
		if FileAccess.file_exists(mabs):
			msrc = "🤖 อบจากโค้ด" if FileAccess.get_sha256(mabs) == String(mmarks.get(id, "")) \
				else "🙌 คนทำ"
		lines.append("| `%s` | %s | %d | %.1f | %s |" % [id, String(WHEN.get(id, "—")),
			int(WQMusic.TRACKS[id]["bpm"]), WQMusic.length_sec(id), msrc])
```

รันใหม่: `godot --headless --path . --script res://audio/tools/sfx_readme.gd`

- [ ] **Step 4: อัปเดตเอกสารสองไฟล์**

`godot/README.md` — เพิ่มในบล็อกคำสั่ง:

```bash
godot --headless --path . --script res://sim/music_check.gd        # 8) เพลงพื้นหลัง
godot --headless --path . --script res://audio/tools/music_bake.gd # อบเพลงเป็น .wav
WQ_MUSIC=crisis godot --path .                                     # ฟังเพลงตัวเดียวแล้วปิดเอง
```

แก้แถว "เสียง" ในตารางสถานะให้รวมเพลง และลบสองบรรทัดนี้ออกจาก "ที่ยังค้างอยู่":
`- ยังไม่มีเพลงพื้นหลัง — ยังไม่มีบัส Music ด้วย ...` (สองบรรทัด) แล้วใส่แทน:

```markdown
- **ยังไม่มีเพลงที่คนทำจริงสักไฟล์** — 0/3 ทุกเพลงเรียบเรียงจากตารางโน้ตในโค้ด
  วางไฟล์ทับได้ทีละเพลง (เช็กลิสต์ใน `audio/README.md`)
```

`godot/CLAUDE.md` — เพิ่มในตารางไฟล์ของข้อ 9:

```markdown
| `audio/music.gd` | `WQMusic` — ตารางโน้ตสามเพลง + ตัวเรียบเรียงเป็น PCM (เพลงเดียวกันต้องได้ไบต์เดิม) |
| `audio/tools/music_bake.gd` | อบเพลงเป็น `.wav` + ตราประทับ sha256 เหมือน sfx |
| `audio/tools/wav_probe.gd` | `WQWavProbe` — อ่าน PCM จากไฟล์ `.wav` ดิบ (ห้ามอ่านผ่าน `load()` มันเป็น QOA) |
| `sim/music_check.gd` | เทสต์ headless ของระบบเพลง |
```

และเพิ่มในหัวข้อ "ที่ของจริงต่างจาก ART-DIRECTION / CLAUDE-CODE-BRIEF":

```markdown
- **จุดลูปของเพลงตั้งจากโค้ดตอนโหลด ไม่ใช่ติดมากับไฟล์** — Godot นำเข้า `.wav` แล้วได้
  `loop_mode = 0` เสมอ ต้อง `duplicate()` แล้วตั้ง `LOOP_FORWARD` เอง (duplicate ก่อนเสมอ
  ไม่งั้นไปแก้ตัวที่ทั้งโปรเจกต์แคชร่วมกัน)
- **หางโน้ตท้ายเพลงถูกวนกลับไปบวกที่ต้นเพลง** ใน `WQMusic._stamp()` ไม่ใช่ตัดทิ้ง
  ถ้าตัดทิ้งจะได้ยิน "ป๊อก" ทุกครั้งที่เพลงครบรอบ
```

- [ ] **Step 5: รันทุกอย่างครั้งสุดท้าย**

```bash
cd godot
for s in rng_check headless_sim save_check ui_check flow_check world_check audio_check music_check; do
  godot --headless --path . --script res://sim/$s.gd >/dev/null 2>&1 || echo "FAILED: $s"
done
WQ_JOB=teacher WQ_TUT=off WQ_SHOT=/tmp/final.png godot --path .
```

Expected: ไม่มี `FAILED:` · ภาพหน้าจอปกติ

- [ ] **Step 6: commit**

```bash
cd "/Users/artnopparat/IT Dept/Tech part/wealth-quest"
git add godot/audio godot/ui/main.gd godot/CLAUDE.md godot/README.md
git commit -m "Godot: เพลงด่าน 2 กับเพลงวิกฤต + เอกสารระบบเพลง"
```

- [ ] **Step 7: 🛑 ส่งให้เจ้าของโปรเจกต์ฟังจริงทั้งสามเพลงในเกม**

```bash
cd godot
for t in phase1 phase2 crisis; do echo "--- $t ---"; WQ_MUSIC=$t godot --path .; done
```

รายงานว่าเพลงไหนต้องแก้ตรงไหน แล้วแก้สตริงใน `WQMusic.TRACKS` → `music_bake -- <id>` → `--import` → ฟังใหม่
**ห้ามสรุปว่างานเสร็จก่อนได้ยินคำตอบจากคนฟังจริง** — ตัวตรวจอัตโนมัติบอกได้แค่ว่ามีเสียงและโครงถูก
