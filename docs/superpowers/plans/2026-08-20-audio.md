# แผนลงมือทำระบบเสียง Wealth Quest

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ให้ Wealth Quest มีเสียงครบทุกเหตุการณ์ 30 เสียง สังเคราะห์จากโค้ดเอง อบเป็น `.wav` จริงบนดิสก์ ยิงจากสัญญาณของ core และปรับ/ปิดเสียงได้จาก HUD

**Architecture:** `audio/synth.gd` สังเคราะห์คลื่น → `audio/bank.gd` เก็บสเปกเสียงทุกตัวและตารางสัญญาณ→เสียงไว้ที่เดียว → `audio/tools/sfx_bake.gd` อบเป็น `audio/sfx/<id>.wav` พร้อมจด sha256 ไว้ใน `baked.json` เพื่อไม่ทับงานคนทำเสียง → `WQAudio` (autoload) ฟังสัญญาณของผู้เล่นคนที่ผูกไว้แล้วเล่นเสียงผ่าน pool 8 ตัว โดยแยกเป็นสองเลนอย่างเด็ดขาด: เหตุการณ์ของเกมมาจากสัญญาณเท่านั้น · เสียง UI ล้วนเรียกผ่าน `WQAudio.ui()`

**Tech Stack:** Godot 4.7.1 · GDScript · `AudioStreamWAV.save_to_wav()` · `ConfigFile` · ไม่มี dependency ภายนอกใดๆ

**Spec:** `docs/superpowers/specs/2026-08-20-audio-design.md`

## Global Constraints

- **Godot 4.7.1** — ตรวจมาแล้วว่า `AudioStreamWAV.save_to_wav()` มีจริงและคืน `err=0`
- **ห้ามเพิ่ม dependency ภายนอก** (กฎ "สิ่งที่ห้ามทำโดยไม่ถามเจ้าของโปรเจกต์ก่อน")
- **`core/` ห้ามรู้จัก `ui/` หรือ `world/`** ห้าม `preload` ห้าม `get_node` — `core/` จึงห้ามอ้าง `WQAudio` เด็ดขาด core ทำได้แค่ "ยิงสัญญาณ" เท่านั้น
- **`sim/` และ `core/` ต้องรันได้โดยไม่มีเสียง** — `--headless` ใช้ไดรเวอร์ `Dummy` และมี `bus_count = 1`
- **แตะ `core/` เมื่อไหร่ ต้องรัน sim ยืนยัน parity ก่อน commit** (กฎเหล็กข้อ 3)
- **ตัวเลขสมดุลอยู่ใน `data/*.json` เท่านั้น** — สเปกเสียงไม่ใช่ตัวเลขสมดุล อยู่ใน `audio/bank.gd` แบบเดียวกับที่สีอยู่ใน `ui/theme/palette.gd`
- **เพิ่ม `class_name` ใหม่แล้วต้องรัน `godot --headless --path . --import` ก่อน** ไม่งั้นจะหา class ไม่เจอ
- **ทุกคำสั่งรันจากโฟลเดอร์ `godot/`** และใส่ `--path .` เสมอ
- **สไตล์ commit ภาษาไทย** — `Godot: …` / `เอกสาร: …` ไม่มี prefix อื่น (`.navenv.json` ตั้ง `commit_tag` เป็นค่าว่างไว้ตั้งใจ)
- **`audio/README.md` สร้างจากสคริปต์เสมอ ห้ามแก้ด้วยมือ** — กติกาเดียวกับ `world/models/README.md`

## รายการไฟล์

| ไฟล์ | หน้าที่ | Task |
|---|---|---|
| `audio/synth.gd` | `WQSynth` — สังเคราะห์ square/triangle/noise + envelope เป็นตัวอย่างเสียง 16-bit | 1 |
| `audio/bank.gd` | `WQBank` — สเปกเสียง 30 ตัว + ตารางสัญญาณ→เสียง (ที่เดียวของทั้งระบบ) | 1 |
| `sim/audio_check.gd` | เทสต์ headless ของทั้งระบบเสียง | 1–6 |
| `audio/tools/sfx_bake.gd` | อบ bank เป็น `audio/sfx/<id>.wav` + จด sha256 | 2 |
| `audio/sfx/*.wav` · `audio/sfx/baked.json` | ไฟล์เสียงจริง + ตราประทับว่าอบจากโค้ด | 2 |
| `core/player.gd` | เพิ่ม `signal acted(kind)` + จุดยิง 16 จุด | 3 |
| `audio/audio.gd` | `WQAudio` — autoload: bus · pool · สองเลน · ระดับเสียง | 4 |
| `project.godot` | ลงทะเบียน autoload `WQAudio` | 4 |
| `ui/main.gd` | ผูก `WQAudio` เข้ากับแมตช์/ผู้เล่น · เสียงเปิด-ปิดแผง · `WQ_SFX` | 5 |
| `ui/widgets/hud.gd` | ปุ่ม 🔊 + เสียงคลิกปุ่มบน HUD | 5 |
| `ui/widgets/action_panel.gd` | เสียง `denied` เมื่อ core ตอบว่าทำไม่ได้ | 5 |
| `ui/widgets/dice.gd` | เสียง `dice_roll` · `dice_land` | 5 |
| `ui/widgets/audio_panel.gd` | `WQAudioPanel` — สไลเดอร์ Master/SFX + ปุ่มปิดเสียง | 6 |
| `audio/tools/sfx_readme.gd` | สร้าง `audio/README.md` | 7 |
| `godot/CLAUDE.md` · `godot/README.md` | กฎใหม่ 4 ข้อ + สถานะ + คำสั่ง | 7 |

---

### Task 1: `WQSynth` + `WQBank` + โครงเทสต์

**Files:**
- Create: `audio/synth.gd`
- Create: `audio/bank.gd`
- Create: `sim/audio_check.gd`

**Interfaces:**
- Consumes: ไม่มี (เป็น task แรก)
- Produces:
  - `WQSynth.RATE: int` = 22050
  - `WQSynth.render(spec: Array) -> PackedByteArray` — ตัวอย่างเสียง 16-bit mono
  - `WQSynth.stream(spec: Array) -> AudioStreamWAV`
  - `WQBank.SPEC: Dictionary` — `id -> [wave, f0, f1, dur, attack, decay, vol]`
  - `WQBank.FROM_ACTED: Dictionary` — `kind -> id`
  - `WQBank.UI_IDS: Array` — id ของเสียงเลน UI
  - `WQBank.ids() -> Array` — id ทั้งหมดเรียงตามตัวอักษร

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

สร้าง `sim/audio_check.gd`:

```gdscript
extends SceneTree
## ตรวจระบบเสียงแบบ headless — ไม่ต้องมีลำโพง
##   godot --headless --path . --script res://sim/audio_check.gd
##
## headless ใช้ไดรเวอร์เสียง `Dummy` เสียงจึงไม่ออกจริง ที่นี่ตรวจได้แค่
## "มีของครบไหม · ยิงถูกจังหวะไหม" ไม่ได้ตรวจว่าฟังแล้วรู้เรื่อง
## ฟังจริงด้วย:  WQ_SFX=<id> godot --path .

var _fails := 0


func _init() -> void:
	_check_bank()
	_check_synth()
	print("audio_check: %s" % ("ผ่านทั้งหมด ✅" if _fails == 0 else "ไม่ผ่าน %d ข้อ ❌" % _fails))
	quit(1 if _fails > 0 else 0)


## ทุก id ที่ตารางสัญญาณอ้างถึง ต้องมีสเปกอยู่จริง ไม่งั้นเกมจะเงียบเฉพาะบางเหตุการณ์
## โดยไม่มี error ให้เห็น — เป็นบั๊กที่หาเจอยากที่สุดของระบบเสียง
func _check_bank() -> void:
	_eq("มีสเปกเสียงครบ 30 ตัว", WQBank.SPEC.size(), 30)
	for kind in WQBank.FROM_ACTED:
		_eq("acted(%s) ชี้ไปเสียงที่มีจริง" % kind,
			WQBank.SPEC.has(String(WQBank.FROM_ACTED[kind])), true)
	for id in WQBank.UI_IDS:
		_eq("เสียง UI \"%s\" มีสเปก" % id, WQBank.SPEC.has(String(id)), true)
	# claim_dream ต้องไม่อยู่ในตาราง ไม่งั้นเสียงชนะจะดังสองครั้ง
	# เพราะ add_champion() ยิง player_finished ตามมาทันทีอยู่แล้ว
	_eq("acted(dream) ต้องไม่มีเสียงของตัวเอง", WQBank.FROM_ACTED.has("dream"), false)
	for id in WQBank.ids():
		var spec: Array = WQBank.SPEC[id]
		_eq("สเปก \"%s\" มี 7 ช่อง" % id, spec.size(), 7)
		_eq("คลื่นของ \"%s\" เป็นแบบที่ synth รู้จัก" % id,
			["square", "triangle", "noise"].has(String(spec[0])), true)
		_eq("\"%s\" ยาวไม่เกิน 2 วินาที" % id, float(spec[3]) <= 2.0, true)


func _check_synth() -> void:
	var spec: Array = WQBank.SPEC["click"]
	var data := WQSynth.render(spec)
	_eq("ความยาวข้อมูล = วินาที × อัตราสุ่ม × 2 ไบต์",
		data.size(), int(WQSynth.RATE * float(spec[3])) * 2)

	# เสียงต้องไม่เงียบสนิท ไม่งั้นอบไฟล์ครบ 30 ตัวแล้วเกมก็ยังเงียบอยู่ดี
	var peak := 0
	for i in int(data.size() / 2.0):
		peak = maxi(peak, absi(data.decode_s16(i * 2)))
	_eq("เสียงไม่เงียบสนิท", peak > 3000, true)

	# อบซ้ำต้องได้ไบต์เดิมเป๊ะ ไม่งั้น sha256 เปลี่ยนทุกครั้งที่รันตัวอบ
	# แล้วท่อ "ไม่ทับงานคน" จะพังทั้งท่อ (noise ต้องใช้ตัวสุ่มที่ตั้ง seed ไว้)
	_eq("สังเคราะห์ซ้ำได้ผลเหมือนเดิม",
		WQSynth.render(WQBank.SPEC["travel"]), WQSynth.render(WQBank.SPEC["travel"]))

	var st := WQSynth.stream(spec)
	_eq("สตรีมเป็น 16-bit", st.format, AudioStreamWAV.FORMAT_16_BITS)
	_eq("สตรีมเป็น mono", st.stereo, false)
	_eq("อัตราสุ่มตรงกับ synth", st.mix_rate, WQSynth.RATE)


func _eq(label: String, got, want) -> void:
	if got == want: return
	_fails += 1
	print("  ❌ %s: ได้ %s ต้องการ %s" % [label, str(got), str(want)])
```

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `Could not find type "WQBank" in the current scope` (ยังไม่มีไฟล์ และยังไม่ได้ `--import`)

- [ ] **Step 3: เขียน `audio/synth.gd`**

```gdscript
class_name WQSynth
extends RefCounted
## เครื่องสังเคราะห์เสียง chiptune — คลื่นสามแบบพอสำหรับทั้งเกม
##
## ตัวนี้คือ `world/tools/meshkit.gd` ของฝั่งเสียง: ตัวต่อพื้นฐานที่ไม่รู้จักเกมเลย
## รู้แค่ว่า "สเปกหนึ่งชุด → ตัวอย่างเสียง" ส่วนเสียงไหนใช้ตอนไหนเป็นเรื่องของ WQBank
##
## **ต้องได้ไบต์เดิมเป๊ะทุกครั้งที่เรียกด้วยสเปกเดียวกัน** — ตัวอบใช้ sha256 แยกว่า
## ไฟล์ไหนอบจากโค้ดไฟล์ไหนคนทำมา ถ้า noise สุ่มใหม่ทุกครั้ง ทุกไฟล์จะดู "เปลี่ยนแล้ว"
## ตลอดเวลา จึงต้องใช้ RandomNumberGenerator ที่ตั้ง seed ไว้ ไม่ใช่ randf() ของ Godot

const RATE := 22050              ## พอสำหรับ chiptune และทำให้ไฟล์เล็ก (~6.6 KB ต่อ 0.15 วิ)
const NOISE_SEED := 20260820     ## คงที่ตลอดกาล — เปลี่ยนเมื่อไหร่ทุกไฟล์ noise ถูกอบใหม่หมด


## สเปก: [คลื่น, ความถี่เริ่ม, ความถี่จบ, ความยาว(วิ), attack(วิ), decay(วิ), ระดับเสียง]
static func render(spec: Array) -> PackedByteArray:
	var wave := String(spec[0])
	var f0 := float(spec[1])
	var f1 := float(spec[2])
	var dur := float(spec[3])
	var attack := float(spec[4])
	var decay := float(spec[5])
	var vol := float(spec[6])

	var n := int(RATE * dur)
	var out := PackedByteArray()
	out.resize(n * 2)

	var rng := RandomNumberGenerator.new()
	rng.seed = NOISE_SEED
	var phase := 0.0
	for i in n:
		var t := float(i) / float(RATE)
		var freq: float = lerpf(f0, f1, t / maxf(dur, 0.0001))
		phase = fmod(phase + freq / float(RATE), 1.0)
		var s := 0.0
		match wave:
			"square": s = 1.0 if phase < 0.5 else -1.0
			"triangle": s = 4.0 * absf(phase - 0.5) - 1.0
			"noise": s = rng.randf_range(-1.0, 1.0)
		var v: float = clampf(s * _env(t, dur, attack, decay) * vol, -1.0, 1.0)
		out.encode_s16(i * 2, int(v * 32767.0))
	return out


static func stream(spec: Array) -> AudioStreamWAV:
	var st := AudioStreamWAV.new()
	st.format = AudioStreamWAV.FORMAT_16_BITS
	st.mix_rate = RATE
	st.stereo = false
	st.data = render(spec)
	return st


## ซองเสียง attack → คงที่ → decay · ไม่มี sustain แยกเพราะเสียงทุกตัวสั้นกว่า 2 วินาที
static func _env(t: float, dur: float, attack: float, decay: float) -> float:
	if t < attack: return t / maxf(attack, 0.0001)
	var rel := dur - decay
	if t >= rel: return maxf(0.0, (dur - t) / maxf(decay, 0.0001))
	return 1.0
```

- [ ] **Step 4: เขียน `audio/bank.gd`**

```gdscript
class_name WQBank
extends RefCounted
## สเปกเสียงทุกตัวของเกม + ตารางว่าสัญญาณไหนทำให้อะไรดัง — **ที่เดียวเท่านั้น**
##
## ตัวนี้คือ `world/tools/model_ids.gd` ของฝั่งเสียง ถ้ารายการนี้กระจายไปหลายที่
## วันหนึ่งจะมีเสียงที่ไม่มีไฟล์ หรือไฟล์ที่ไม่มีใครเล่น โดยไม่มี error ให้เห็น
##
## สเปก: [คลื่น, ความถี่เริ่ม(Hz), ความถี่จบ(Hz), ความยาว(วิ), attack(วิ), decay(วิ), ระดับเสียง]
## ความถี่เริ่ม≠จบ = เสียงกวาดขึ้นหรือลง (ขึ้น = ได้ · ลง = เสีย) เป็นกติกาเดียวของทั้งชุด

const SPEC := {
	# --- เงิน · ขึ้น = ได้เงิน ลง = จ่ายเงิน ---
	"payday":      ["triangle", 523.0, 1046.0, 0.45, 0.01, 0.25, 0.55],
	"deal_closed": ["triangle", 659.0, 1318.0, 0.40, 0.01, 0.22, 0.55],
	"sell":        ["triangle", 784.0, 523.0, 0.30, 0.01, 0.18, 0.50],
	"loan":        ["square", 392.0, 262.0, 0.35, 0.01, 0.20, 0.40],
	"repay":       ["square", 392.0, 587.0, 0.30, 0.01, 0.18, 0.40],
	"buy":         ["square", 587.0, 880.0, 0.22, 0.005, 0.12, 0.45],

	# --- ลงมือทำ · โทนกลางๆ สั้น ไม่แย่งความสนใจจากเสียงเงิน ---
	"ot":          ["square", 330.0, 392.0, 0.20, 0.005, 0.10, 0.35],
	"freelance":   ["square", 440.0, 523.0, 0.20, 0.005, 0.10, 0.35],
	"scout":       ["square", 523.0, 698.0, 0.24, 0.005, 0.12, 0.35],
	"study":       ["triangle", 440.0, 659.0, 0.35, 0.01, 0.18, 0.40],
	"rest":        ["triangle", 330.0, 262.0, 0.40, 0.02, 0.25, 0.35],
	"gym":         ["square", 262.0, 349.0, 0.25, 0.005, 0.12, 0.40],
	"resort":      ["triangle", 392.0, 523.0, 0.55, 0.02, 0.30, 0.40],
	"lifestyle":   ["square", 349.0, 440.0, 0.16, 0.005, 0.08, 0.30],

	# --- เดินทาง ---
	# "arrive" (เดินถึงที่หมาย) ถูกตัดทิ้งตั้งแต่ต้น — แอนิเมชันเดินข้ามได้ (กฎ world/ ข้อ 4)
	# เสียงจะดังบ้างไม่ดังบ้างโดยผู้เล่นไม่มีทางรู้ว่าทำไม · การเดินทางมีเสียงเดียวคือตอนกดปุ่ม
	"travel":      ["noise", 200.0, 200.0, 0.30, 0.02, 0.20, 0.25],

	# --- จังหวะเกม ---
	"month_end":   ["triangle", 262.0, 392.0, 0.50, 0.02, 0.28, 0.45],
	"dice_roll":   ["noise", 400.0, 400.0, 0.35, 0.01, 0.20, 0.30],
	"dice_land":   ["square", 880.0, 880.0, 0.12, 0.003, 0.08, 0.45],
	"disaster":    ["noise", 120.0, 60.0, 0.70, 0.01, 0.40, 0.55],
	"health_low":  ["square", 330.0, 220.0, 0.60, 0.01, 0.35, 0.45],
	"phase2":      ["triangle", 523.0, 1046.0, 0.70, 0.02, 0.35, 0.55],
	"win":         ["triangle", 523.0, 1568.0, 1.20, 0.02, 0.60, 0.65],
	"lose":        ["triangle", 392.0, 131.0, 1.00, 0.02, 0.55, 0.50],

	# --- UI · เบาที่สุดของทั้งชุด เพราะดังบ่อยที่สุด ---
	"click":         ["square", 880.0, 880.0, 0.04, 0.002, 0.03, 0.25],
	"panel_open":    ["square", 659.0, 880.0, 0.10, 0.003, 0.06, 0.25],
	"panel_close":   ["square", 880.0, 659.0, 0.10, 0.003, 0.06, 0.25],
	"denied":        ["square", 220.0, 165.0, 0.16, 0.003, 0.10, 0.35],
	"tutorial_step": ["triangle", 698.0, 880.0, 0.18, 0.005, 0.10, 0.30],
	"save":          ["square", 523.0, 698.0, 0.14, 0.003, 0.08, 0.30],
	"load":          ["square", 698.0, 523.0, 0.14, 0.003, 0.08, 0.30],
}

## `WQPlayer.acted(kind)` → id เสียง
##
## **"dream" ไม่อยู่ในนี้โดยตั้งใจ** — `claim_dream()` เรียก `match_ref.add_champion()`
## ซึ่งยิง `player_finished` ตามมาทันที ถ้าใส่ไว้ด้วยเสียงชนะจะดังซ้อนกันสองครั้ง
const FROM_ACTED := {
	"travel": "travel", "buy": "buy", "sell": "sell", "loan": "loan", "repay": "repay",
	"ot": "ot", "freelance": "freelance", "scout": "scout", "study": "study",
	"gym": "gym", "resort": "resort", "rest": "rest", "lifestyle": "lifestyle",
	"phase2": "phase2",
}

## เสียงที่ `ui/` เรียกเองได้ผ่าน `WQAudio.ui()` — นอกรายการนี้ต้องมาจากสัญญาณเท่านั้น
##
## ลูกเต๋าอยู่ในเลนนี้เพราะมันคือ **ภาพเคลื่อนไหว** ไม่ใช่การเปลี่ยนสถานะ —
## แต้มถูกทอยจาก core ไปเรียบร้อยแล้วก่อนที่ลูกเต๋าจะเริ่มหมุนบนจอ
const UI_IDS := ["click", "panel_open", "panel_close", "denied", "tutorial_step",
	"save", "load", "dice_roll", "dice_land"]


static func ids() -> Array:
	var out := SPEC.keys()
	out.sort()
	return out
```

- [ ] **Step 5: รัน `--import` แล้วรันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅` และ exit code 0

- [ ] **Step 6: commit**

```bash
git add godot/audio/synth.gd godot/audio/bank.gd godot/sim/audio_check.gd godot/audio/*.uid godot/sim/*.uid
git commit -m "Godot: เครื่องสังเคราะห์เสียง + สเปกเสียง 30 ตัว (ยังไม่มีไฟล์ .wav)"
```

---

### Task 2: ตัวอบ `.wav` + ไฟล์เสียงจริง 30 ไฟล์

**Files:**
- Create: `audio/tools/sfx_bake.gd`
- Create: `audio/sfx/*.wav` (30 ไฟล์ · ผลจากการรันตัวอบ) · `audio/sfx/baked.json`
- Modify: `sim/audio_check.gd` — เพิ่ม `_check_files()`

**Interfaces:**
- Consumes: `WQBank.ids()` · `WQBank.SPEC` · `WQSynth.stream()`
- Produces:
  - `audio/sfx/<id>.wav` สำหรับทุก id ใน `WQBank.ids()`
  - `audio/sfx/baked.json` = `{ "<id>": "<sha256>" }`

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

เพิ่มใน `sim/audio_check.gd` — เรียก `_check_files()` ต่อจาก `_check_synth()` ใน `_init()`:

```gdscript
const SFX_DIR := "res://audio/sfx"


## ไฟล์เสียงต้องครบและต้องไม่มีไฟล์กำพร้า
## ไฟล์กำพร้า = เคยมี id นี้แล้วลบออกจาก bank แต่ลืมลบไฟล์ → repo โตขึ้นเรื่อยๆ โดยไม่มีใครเล่น
func _check_files() -> void:
	var on_disk := {}
	var d := DirAccess.open(SFX_DIR)
	if d == null:
		_fails += 1
		print("  ❌ ไม่มีโฟลเดอร์ %s" % SFX_DIR)
		return
	for f in d.get_files():
		if f.ends_with(".wav"): on_disk[f.get_basename()] = true

	for id in WQBank.ids():
		var path := "%s/%s.wav" % [SFX_DIR, id]
		_eq("มีไฟล์เสียง \"%s\"" % id, ResourceLoader.exists(path), true)
		if not ResourceLoader.exists(path): continue
		var st = load(path)
		_eq("\"%s\" โหลดเป็น AudioStreamWAV ได้" % id, st is AudioStreamWAV, true)
		on_disk.erase(id)

	_eq("ไม่มีไฟล์เสียงกำพร้า", on_disk.keys(), [])

	var stamp := FileAccess.open("%s/baked.json" % SFX_DIR, FileAccess.READ)
	_eq("มี baked.json", stamp != null, true)
	if stamp == null: return
	var marks = JSON.parse_string(stamp.get_as_text())
	stamp.close()
	_eq("baked.json จดครบทุก id", marks is Dictionary and marks.size() == WQBank.ids().size(), true)
```

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `❌ ไม่มีโฟลเดอร์ res://audio/sfx`

- [ ] **Step 3: เขียน `audio/tools/sfx_bake.gd`**

```gdscript
extends SceneTree
## อบสเปกใน WQBank ออกเป็นไฟล์ `.wav` จริง — ไม่ต้องใช้โปรแกรมทำเสียงข้างนอก
##   godot --headless --path . --script res://audio/tools/sfx_bake.gd
##   godot --headless --path . --script res://audio/tools/sfx_bake.gd -- click travel
##   WQ_SFX_FORCE=1 ...        เขียนทับแม้กระทั่งไฟล์ที่คนทำเสียงวางไว้
##
## **ไฟล์ที่ออกมาไม่ใช่เสียงที่คนทำ** — มันคือคลื่นสังเคราะห์จาก WQSynth
## ประโยชน์คือท่อเดินครบวง: อบ → นำเข้า → เกมมีเสียง → คนทำเสียงวางไฟล์ทับทีละตัวได้เลย
##
## `save_to_wav()` เขียนแค่ chunk fmt กับ data ไม่มีช่องใส่ตราประทับแบบที่ glTF มี `copyright`
## จึงจด sha256 ของไฟล์ที่อบเองไว้ใน baked.json แทน — ถ้าแฮชของไฟล์บนดิสก์ไม่ตรงกับที่จด
## แปลว่ามีคนวางไฟล์จริงทับ **ตัวอบจะไม่แตะไฟล์นั้นเด็ดขาด**

const SFX_DIR := "res://audio/sfx"
const STAMP := "res://audio/sfx/baked.json"
const TMP := "user://sfx_bake_tmp.wav"

var _written := 0
var _skipped_human := 0
var _skipped_same := 0
var _fails := 0


func _init() -> void:
	var only := _wanted()
	var force := OS.get_environment("WQ_SFX_FORCE") != ""
	print("=== sfx_bake%s ===" % ("" if only.is_empty() else " (เฉพาะ %d ตัว)" % only.size()))

	DirAccess.make_dir_recursive_absolute(ProjectSettings.globalize_path(SFX_DIR))
	var marks := _load_marks()

	for id in WQBank.ids():
		if not only.is_empty() and not only.has(id): continue
		_bake(String(id), marks, force)

	_save_marks(marks)
	print("sfx_bake: เขียน %d ตัว · ข้ามของที่คนทำ %d · ไม่เปลี่ยน %d%s" % [
		_written, _skipped_human, _skipped_same,
		"" if _fails == 0 else " · ล้มเหลว %d ❌" % _fails])
	if _written > 0:
		print("  อย่าลืม: godot --headless --path . --import   แล้วรัน audio_check")
	quit(1 if _fails > 0 else 0)


func _wanted() -> Array:
	var out: Array = []
	for a in OS.get_cmdline_user_args(): out.append(String(a))
	return out


func _bake(id: String, marks: Dictionary, force: bool) -> void:
	var path := "%s/%s.wav" % [SFX_DIR, id]
	var abs := ProjectSettings.globalize_path(path)
	var tmp_abs := ProjectSettings.globalize_path(TMP)

	# อบลงที่พักก่อน เพื่อเทียบแฮชได้โดยไม่ต้องแตะไฟล์จริง
	var err := WQSynth.stream(WQBank.SPEC[id]).save_to_wav(TMP)
	if err != OK:
		_fails += 1
		print("  ❌ %s — เขียนไฟล์ที่พักไม่ได้ (error %d)" % [id, err])
		return
	var fresh := FileAccess.get_sha256(tmp_abs)

	if FileAccess.file_exists(abs):
		var have := FileAccess.get_sha256(abs)
		if have != String(marks.get(id, "")) and not force:
			_skipped_human += 1
			print("  🙌 %s — มีเสียงที่คนทำอยู่แล้ว ไม่แตะ" % id)
			DirAccess.remove_absolute(tmp_abs)
			return
		if have == fresh:
			_skipped_same += 1
			marks[id] = fresh
			DirAccess.remove_absolute(tmp_abs)
			return

	if DirAccess.copy_absolute(tmp_abs, abs) != OK:
		_fails += 1
		print("  ❌ %s — ก๊อปจากที่พักไม่ได้" % id)
		return
	DirAccess.remove_absolute(tmp_abs)
	marks[id] = fresh
	_written += 1
	print("  ✅ %s -> %s" % [id, path])


func _load_marks() -> Dictionary:
	var f := FileAccess.open(STAMP, FileAccess.READ)
	if f == null: return {}
	var parsed = JSON.parse_string(f.get_as_text())
	f.close()
	return parsed if parsed is Dictionary else {}


func _save_marks(marks: Dictionary) -> void:
	var f := FileAccess.open(STAMP, FileAccess.WRITE)
	if f == null:
		_fails += 1
		print("  ❌ เขียน baked.json ไม่ได้")
		return
	f.store_string(JSON.stringify(marks, "\t", true))
	f.close()
```

- [ ] **Step 4: อบไฟล์แล้วนำเข้า**

```bash
cd godot && godot --headless --path . --script res://audio/tools/sfx_bake.gd && godot --headless --path . --import
```

Expected: `sfx_bake: เขียน 30 ตัว · ข้ามของที่คนทำ 0 · ไม่เปลี่ยน 0`

- [ ] **Step 5: รันซ้ำเพื่อพิสูจน์ว่าอบซ้ำแล้วไม่เปลี่ยนอะไร**

```bash
cd godot && godot --headless --path . --script res://audio/tools/sfx_bake.gd
```

Expected: `sfx_bake: เขียน 0 ตัว · ข้ามของที่คนทำ 0 · ไม่เปลี่ยน 30`
ถ้าได้ `เขียน 30` แปลว่า `WQSynth` ยังสังเคราะห์ไม่นิ่ง (มักเพราะ noise ไม่ได้ตั้ง seed) — กลับไปแก้ Task 1

- [ ] **Step 6: พิสูจน์ว่าไฟล์ของคนทำเสียงไม่ถูกทับ**

```bash
cd godot && cp audio/sfx/click.wav /tmp/click_backup.wav && printf 'human' >> audio/sfx/click.wav
godot --headless --path . --script res://audio/tools/sfx_bake.gd
```

Expected: บรรทัด `🙌 click — มีเสียงที่คนทำอยู่แล้ว ไม่แตะ` และสรุป `ข้ามของที่คนทำ 1`

คืนไฟล์เดิม:
```bash
cd godot && cp /tmp/click_backup.wav audio/sfx/click.wav
```

- [ ] **Step 7: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅`

- [ ] **Step 8: commit**

```bash
git add godot/audio/tools/sfx_bake.gd godot/audio/sfx godot/sim/audio_check.gd godot/audio/tools/*.uid
git commit -m "Godot: อบเสียง 30 ตัวเป็นไฟล์ .wav จริง + ตราประทับกันทับงานคนทำเสียง"
```

---

### Task 3: `acted(kind)` ใน `core/player.gd`

**Files:**
- Modify: `core/player.gd` — เพิ่มสัญญาณ 1 ตัว + จุดยิง 16 จุด
- Modify: `sim/audio_check.gd` — เพิ่ม `_check_acted()`

**Interfaces:**
- Consumes: ไม่มี (แก้ core ล้วน)
- Produces: `WQPlayer.acted(kind: String)` — `kind` เป็นค่าใดค่าหนึ่งใน: `travel` · `buy` · `sell` · `loan` · `repay` · `ot` · `freelance` · `scout` · `study` · `gym` · `resort` · `rest` · `lifestyle` · `dream` · `phase2`

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

เพิ่มใน `sim/audio_check.gd` — เรียก `_check_acted()` ต่อจาก `_check_files()`:

```gdscript
## `acted` ต้องยิงเมื่อทำสำเร็จ และ **ห้ามยิงเมื่อล้มเหลว**
## ถ้ายิงตอนล้มเหลวด้วย ผู้เล่นจะได้ยินเสียง "กู้เงินสำเร็จ" ทั้งที่วงเงินไม่พอ
func _check_acted() -> void:
	WQData.load_all()
	var m := WQMatch.new()
	m.setup({"mode": "solo", "seed": 20260815,
		"players": [{"name": "คุณ", "job_id": "teacher", "is_ai": false}]})
	var p = m.players[0]

	var heard: Array[String] = []
	p.acted.connect(func(kind: String): heard.append(kind))

	# --- สำเร็จ ---
	p.place = "home"
	p.rest()
	_eq("rest สำเร็จแล้วยิง acted", heard, ["rest"] as Array[String])

	heard.clear()
	p.travel_to("office")
	_eq("travel_to สำเร็จแล้วยิง acted", heard, ["travel"] as Array[String])

	heard.clear()
	p.set_sleep(0)          # อยู่ที่ออฟฟิศ ตั้งค่าการนอนไม่ได้ → ต้องเงียบ
	_eq("set_sleep ผิดที่แล้วต้องไม่ยิง", heard, [] as Array[String])

	# --- ล้มเหลว: เวลาหมด ---
	heard.clear()
	p.hours = 0
	_eq("side_job เวลาไม่พอต้องคืน ok=false", bool(p.side_job("ot").get("ok", true)), false)
	_eq("ล้มเหลวแล้วต้องไม่ยิง acted", heard, [] as Array[String])

	# --- ล้มเหลว: เงินไม่พอ ---
	heard.clear()
	p.hours = p.get_hours_max()
	p.cash = 0.0
	p.place = "mall"
	_eq("ซื้อพาหนะเงินไม่พอต้องคืน ok=false",
		bool(p.buy_vehicle("luxury").get("ok", true)), false)
	_eq("ซื้อไม่สำเร็จแล้วต้องไม่ยิง acted", heard, [] as Array[String])

	# ทุก kind ที่ core ยิงได้ ต้องมีที่อยู่ในตาราง (ยกเว้น dream ที่ตั้งใจไม่ให้มีเสียง)
	for kind in ["travel", "buy", "sell", "loan", "repay", "ot", "freelance",
			"scout", "study", "gym", "resort", "rest", "lifestyle", "phase2"]:
		_eq("kind \"%s\" มีที่อยู่ในตาราง" % kind, WQBank.FROM_ACTED.has(kind), true)
```

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `Invalid access to property or key 'acted'` (ยังไม่มีสัญญาณ)

- [ ] **Step 3: เพิ่มสัญญาณใน `core/player.gd`**

ใต้ `signal deal_closed(deal: Dictionary)` (บรรทัด 10):

```gdscript
## การกระทำที่ทำ **สำเร็จ** แล้ว — เสียงและเอฟเฟกต์ฟังตัวนี้
##
## มีเพราะทุกการกระทำยิงแค่ `changed` เหมือนกันหมด คนฟังจึงแยกไม่ออกว่าเกิดอะไรขึ้น
## **ห้ามยิงตอนคืน `_fail()`** ไม่งั้นผู้เล่นจะได้ยินเสียง "สำเร็จ" ตอนที่ทำไม่ได้
## `core/` ไม่รู้จักระบบเสียงเลย มันแค่ประกาศว่าเกิดอะไรขึ้น ใครจะฟังเป็นเรื่องของคนนั้น
signal acted(kind: String)
```

- [ ] **Step 4: ยิง `acted` ที่จุดสำเร็จทั้ง 16 จุด**

ใส่ **ถัดจาก `changed.emit()`** ของแต่ละเมธอด ก่อน `return {"ok": true}`:

| เมธอด | บรรทัดที่ต้องเพิ่ม |
|---|---|
| `set_sleep` | `acted.emit("lifestyle")` |
| `set_food` | `acted.emit("lifestyle")` |
| `travel_to` | `acted.emit("travel")` |
| `buy_vehicle` — **ทางลดระดับรถ** (ท้ายบล็อก `if WQData.vehicle_index(...) < ...`) | `acted.emit("buy")` |
| `buy_vehicle` — **ทางซื้อปกติ** (ท้ายฟังก์ชัน) | `acted.emit("buy")` |
| `buy_device` | `acted.emit("buy")` |
| `sell_asset` | `acted.emit("sell")` |
| `take_loan` | `acted.emit("loan")` |
| `repay_debt` | `acted.emit("repay")` |
| `side_job` | `acted.emit("ot" if kind == "ot" else "freelance")` |
| `scout` | `acted.emit("scout")` |
| `study` | `acted.emit("study")` |
| `exercise` | `acted.emit("gym")` |
| `vacation` | `acted.emit("resort")` |
| `rest` | `acted.emit("rest")` |
| `claim_dream` | `acted.emit("dream")` |
| `enter_phase2` | `acted.emit("phase2")` |

ตัวอย่างของ `rest()` หลังแก้:

```gdscript
func rest() -> Dictionary:
	if not can_do_here("rest"): return _fail("ต้องกลับ %s ก่อน" % place_name("rest"))
	var c := int(WQData.cfg.action_cost.rest)
	if not can_spend(c): return _fail("เวลาไม่พอ ต้องใช้ %d ชม." % c)
	hours -= c
	health = clampf(health + 2.5, 0, 100)
	rested_this_month = true
	changed.emit()
	acted.emit("rest")
	return {"ok": true}
```

**`buy_vehicle` มีทางออกที่สำเร็จสองทาง** — ทางลดระดับรถ `return {"ok": true}` กลางฟังก์ชัน
ถ้าใส่แค่ท้ายฟังก์ชัน การขายรถลงเพื่อเอาเงินสดจะเงียบสนิททั้งที่เป็นการตัดสินใจใหญ่:

```gdscript
	if WQData.vehicle_index(v.id) < WQData.vehicle_index(cur.id):
		cash += roundf(float(cur.price) * float(cfg.vehicle_downgrade_refund))
		vehicle = v.id
		_log("ขาย %s %s แล้วเปลี่ยนเป็น %s %s" % [cur.icon, cur.name, v.icon, v.name], "sell")
		changed.emit()
		acted.emit("buy")
		return {"ok": true}
```

`enter_phase2()` คืน `void` ไม่ใช่ Dictionary — ใส่ต่อจาก `changed.emit()` ท้ายฟังก์ชันเหมือนกัน

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅`

- [ ] **Step 6: พิสูจน์ว่า parity ไม่ขยับ (กฎเหล็กข้อ 3)**

```bash
cd godot
godot --headless --path . --script res://sim/rng_check.gd
godot --headless --path . --script res://sim/headless_sim.gd
godot --headless --path . --script res://sim/parity_dump.gd -- 60
godot --headless --path . --script res://sim/save_check.gd
```

Expected: ทั้งสี่ตัวผ่านเหมือนก่อนแก้ · `headless_sim` มัธยฐานยังอยู่ 40–60 เดือน · `parity_dump` ยังตรงกับ `engine.js` ทุกช่อง
ถ้ามีตัวไหนขยับ **ให้หยุดทันที** — แปลว่าเผลอย้ายลำดับคำสั่งใน core ไม่ใช่แค่เพิ่มสัญญาณ

- [ ] **Step 7: commit**

```bash
git add godot/core/player.gd godot/sim/audio_check.gd
git commit -m "Godot: core ประกาศ acted(kind) — แยกออกได้ว่าการกระทำไหนสำเร็จ"
```

---

### Task 4: `WQAudio` — autoload + สองเลน

**Files:**
- Create: `audio/audio.gd`
- Modify: `project.godot` — เพิ่มหัวข้อ `[autoload]`
- Modify: `sim/audio_check.gd` — เพิ่ม `_check_audio()`

**Interfaces:**
- Consumes: `WQBank.FROM_ACTED` · `WQBank.UI_IDS` · `WQPlayer.acted` · `WQMatch.month_ended` / `disaster_started` / `player_finished` / `match_over`
- Produces:
  - `WQAudio.bind(match_ref: WQMatch) -> void`
  - `WQAudio.bind_player(player) -> void`
  - `WQAudio.ui(id: String) -> void` — เลน UI เท่านั้น
  - `WQAudio.played: Array[String]` — ประวัติการเล่น (ให้เทสต์อ่าน)
  - `WQAudio.set_level(bus: String, v: float) -> void` · `WQAudio.get_level(bus: String) -> float`
  - `WQAudio.set_muted(v: bool) -> void` · `WQAudio.muted: bool`

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

เพิ่มใน `sim/audio_check.gd` — `_init()` ต้องเป็น `async` เพื่อรอเฟรม (ดูหมายเหตุใต้โค้ด):

```gdscript
## เสียงต้องดังเฉพาะของผู้เล่นคนที่ผูกไว้ ไม่ใช่ของบอท
## เกมมีบอทสามตัวทำครบทุกอย่างทุกเดือน ถ้าฟังทุกคนจะได้ยินเสียงรัวตลอดเวลาจนไร้ความหมาย
func _check_audio() -> void:
	var m := WQMatch.new()
	m.setup({"mode": "solo", "seed": 20260815, "players": [
		{"name": "คุณ", "job_id": "teacher", "is_ai": false},
		{"name": "บอท A", "job_id": "programmer", "is_ai": true},
	]})
	var me = m.players[0]
	var bot = m.players[1]

	WQAudio.bind(m)
	WQAudio.bind_player(me)

	# บัสต้องมีจริง แม้ headless จะเริ่มมาแค่บัสเดียว
	_eq("มีบัส SFX", AudioServer.get_bus_index("SFX") >= 0, true)
	_eq("pool มี 8 ตัว", WQAudio._players.size(), 8)

	WQAudio.played.clear()
	me.place = "home"
	me.rest()
	_eq("acted ของเราทำให้เสียงดัง", WQAudio.played, ["rest"] as Array[String])

	WQAudio.played.clear()
	bot.place = "home"
	bot.rest()
	_eq("acted ของบอทต้องเงียบ", WQAudio.played, [] as Array[String])

	# เลน UI
	WQAudio.played.clear()
	WQAudio.ui("click")
	_eq("ui() เล่นเสียงในรายการได้", WQAudio.played, ["click"] as Array[String])

	WQAudio.played.clear()
	WQAudio.ui("win")     # ไม่อยู่ใน UI_IDS — ต้องถูกปฏิเสธ
	_eq("ui() เล่นเสียงเหตุการณ์ของเกมไม่ได้", WQAudio.played, [] as Array[String])

	# ปิดเสียงแล้วต้องไม่มีอะไรดัง
	WQAudio.set_muted(true)
	WQAudio.played.clear()
	me.hours = me.get_hours_max()
	me.rest()
	WQAudio.ui("click")
	_eq("ปิดเสียงแล้วเงียบสนิท", WQAudio.played, [] as Array[String])
	WQAudio.set_muted(false)

	# ยิงรัวแล้ว pool ต้องไม่งอก
	for _i in 100: WQAudio.ui("click")
	_eq("pool ไม่โตแม้ยิงรัว 100 ครั้ง", WQAudio._players.size(), 8)

	# ระดับเสียงต้องแปลงเป็น dB ลงบัสจริง
	WQAudio.set_level("SFX", 0.5)
	_eq("ระดับเสียงลงบัสจริง",
		snappedf(AudioServer.get_bus_volume_db(AudioServer.get_bus_index("SFX")), 0.01),
		snappedf(linear_to_db(0.5), 0.01))
	WQAudio.set_level("SFX", 1.0)
```

> **หมายเหตุสำคัญ:** ตอนตรวจกับ Godot จริงพบว่าในสคริปต์ที่ `extends SceneTree`
> เรียก `play()` ตั้งแต่ `_init()` ไม่ได้ (`Playback can only happen when a node is inside the scene tree`)
> `_init()` ของ `audio_check.gd` จึงต้องรอเฟรมก่อนเรียก `_check_audio()`:
>
> ```gdscript
> func _init() -> void:
> 	_check_bank()
> 	_check_synth()
> 	_check_files()
> 	_check_acted()
> 	await process_frame        # ← autoload และ pool ต้องเข้า tree ก่อน
> 	_check_audio()
> 	print("audio_check: %s" % ("ผ่านทั้งหมด ✅" if _fails == 0 else "ไม่ผ่าน %d ข้อ ❌" % _fails))
> 	quit(1 if _fails > 0 else 0)
> ```

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `Identifier "WQAudio" not declared in the current scope`

- [ ] **Step 3: เขียน `audio/audio.gd`**

```gdscript
extends Node
## WQAudio — autoload ตัวเดียวของโปรเจกต์ (ลงทะเบียนใน project.godot)
##
## ที่ต้องเป็น autoload: เสียงคลิกต้องเรียกได้จากทุก widget ถ้าเป็นโหนดใน Main.tscn
## ทุกวิดเจ็ตจะต้องไต่ get_node ขึ้นไปหาพ่อ ซึ่งเปราะและผิดสไตล์ที่วิดเจ็ตทุกตัวในเกมนี้
## เป็นอิสระจากกัน (bind ของใครของมัน)
##
## **สองเลน — ห้ามปนกัน:**
##   เหตุการณ์ของเกม → มาจากสัญญาณเท่านั้น ไม่มี API ให้ ui/ สั่งยิง (กฎเดียวกับ world/city/vfx.gd)
##   เสียง UI ล้วน   → widget เรียก WQAudio.ui("click") ได้ เฉพาะ id ใน WQBank.UI_IDS
## ถ้าปล่อยให้ ui/ ยิงเสียงเหตุการณ์เองได้ วันหนึ่งจะมีเสียง "ปิดดีลสำเร็จ" ดังตอนเงินไม่พอ
##
## เสียงดังเฉพาะของผู้เล่นที่ bind_player() ไว้ — บอทเรียกเมธอดชุดเดียวกันทั้งหมด
## ถ้าฟังทุกคนจะได้ยินเสียงรัวตลอดเวลาจนไม่มีความหมาย

const POOL := 8                  ## เสียงซ้อนกันได้ 8 ตัว พอสำหรับเกมที่ไม่มีอะไรยิงรัว
const COOLDOWN := 0.06           ## กันเสียงเดิมซ้อนตัวเองตอนกดปุ่มถี่ๆ (วินาที)
const SFX_DIR := "res://audio/sfx"
const SETTINGS := "user://settings.cfg"

var played: Array[String] = []   ## ประวัติการเล่น — audio_check ใช้ตรวจว่าสัญญาณไหนทำให้อะไรดัง
var muted := false

var _levels := {"Master": 1.0, "SFX": 1.0}
var _players: Array[AudioStreamPlayer] = []
var _cache := {}                 ## id -> AudioStreamWAV
var _last := {}                  ## id -> เวลาที่เล่นครั้งล่าสุด
var _next := 0
var _silent := false             ## headless หรือ WQ_MUTE=1 — ไม่แตะเซิร์ฟเวอร์เสียงเลย
var _match: WQMatch
var _player = null
var _was_critical := false       ## กันเสียงเตือนสุขภาพดังซ้ำทุกครั้งที่ changed


func _ready() -> void:
	_silent = OS.get_environment("WQ_MUTE") != ""
	_ensure_bus()
	for _i in POOL:
		var p := AudioStreamPlayer.new()
		p.bus = "SFX"
		add_child(p)
		_players.append(p)
	_load_settings()


## headless เริ่มมาด้วยบัสเดียว (ตรวจแล้ว: bus_count = 1) จึงสร้างบัส SFX เองแทนการ
## พึ่ง default_bus_layout.tres — ได้ผลเหมือนกันทุกโหมดและเทสต์ headless ได้จริง
func _ensure_bus() -> void:
	if AudioServer.get_bus_index("SFX") >= 0: return
	var idx := AudioServer.bus_count
	AudioServer.add_bus(idx)
	AudioServer.set_bus_name(idx, "SFX")
	AudioServer.set_bus_send(idx, "Master")


# ========== ผูกกับเกม ==========

func bind(match_ref: WQMatch) -> void:
	if _match == match_ref: return
	if _match != null:
		if _match.month_ended.is_connected(_on_month_ended):
			_match.month_ended.disconnect(_on_month_ended)
		if _match.disaster_started.is_connected(_on_disaster):
			_match.disaster_started.disconnect(_on_disaster)
		if _match.player_finished.is_connected(_on_finished):
			_match.player_finished.disconnect(_on_finished)
		if _match.match_over.is_connected(_on_over):
			_match.match_over.disconnect(_on_over)
	_match = match_ref
	if _match != null:
		_match.month_ended.connect(_on_month_ended)
		_match.disaster_started.connect(_on_disaster)
		_match.player_finished.connect(_on_finished)
		_match.match_over.connect(_on_over)


func bind_player(player) -> void:
	if _player == player: return
	if _player != null:
		if _player.acted.is_connected(_on_acted): _player.acted.disconnect(_on_acted)
		if _player.deal_closed.is_connected(_on_deal): _player.deal_closed.disconnect(_on_deal)
		if _player.changed.is_connected(_on_changed): _player.changed.disconnect(_on_changed)
	_player = player
	_was_critical = false
	if _player != null:
		_player.acted.connect(_on_acted)
		_player.deal_closed.connect(_on_deal)
		_player.changed.connect(_on_changed)


func _on_acted(kind: String) -> void:
	var id: String = String(WQBank.FROM_ACTED.get(kind, ""))
	if id != "": _play(id)


func _on_deal(_deal: Dictionary) -> void:
	_play("deal_closed")


## เงินเดือนออกดังเฉพาะเดือนที่เหลือเก็บเป็นบวก — เกณฑ์เดียวกับที่ vfx.gd ใช้
## เดือนที่ติดลบแล้วมีเสียงเหรียญจะสอนผู้เล่นผิดว่าเดือนนี้ผ่านไปด้วยดี
func _on_month_ended(_month: int) -> void:
	_play("month_end")
	if _player == null: return
	if _player.get_total_income() - _player.get_total_expenses() > 0.0: _play("payday")


func _on_disaster(_def: Dictionary) -> void:
	_play("disaster")


func _on_finished(p) -> void:
	if p == _player: _play("win")


func _on_over() -> void:
	if _player != null and int(_player.phase) < 3: _play("lose")


## เตือนสุขภาพ **ครั้งเดียวตอนข้ามเข้าโซนวิกฤต** ไม่ใช่ทุกครั้งที่ changed ยิง
## `changed` ยิงหลายสิบครั้งต่อเดือน ถ้าเล่นทุกครั้งจะกลายเป็นเสียงหอนไม่หยุด
func _on_changed() -> void:
	if _player == null: return
	var crit: bool = float(_player.health) < 40.0
	if crit and not _was_critical: _play("health_low")
	_was_critical = crit


# ========== เลน UI ==========

## เสียงที่ widget เรียกเองได้ — เฉพาะ id ใน WQBank.UI_IDS เท่านั้น
## เหตุการณ์ของเกมต้องมาจากสัญญาณ ไม่ใช่จากที่นี่
func ui(id: String) -> void:
	if not WQBank.UI_IDS.has(id):
		push_warning("WQAudio.ui(\"%s\") — ไม่ใช่เสียงเลน UI ต้องมาจากสัญญาณ" % id)
		return
	_play(id)


# ========== ระดับเสียง ==========

func set_level(bus: String, v: float) -> void:
	if not _levels.has(bus): return
	_levels[bus] = clampf(v, 0.0, 1.0)
	_apply_levels()
	_save_settings()


func get_level(bus: String) -> float:
	return float(_levels.get(bus, 1.0))


func set_muted(v: bool) -> void:
	muted = v
	_apply_levels()
	_save_settings()


func _apply_levels() -> void:
	if _silent: return
	for bus in _levels:
		var idx := AudioServer.get_bus_index(String(bus))
		if idx < 0: continue
		AudioServer.set_bus_volume_db(idx, linear_to_db(maxf(float(_levels[bus]), 0.0001)))
		AudioServer.set_bus_mute(idx, muted)


func _load_settings() -> void:
	var cfg := ConfigFile.new()
	if cfg.load(SETTINGS) == OK:
		_levels["Master"] = float(cfg.get_value("audio", "master", 1.0))
		_levels["SFX"] = float(cfg.get_value("audio", "sfx", 1.0))
		muted = bool(cfg.get_value("audio", "muted", false))
	_apply_levels()


## เก็บแยกจากไฟล์เซฟโดยตั้งใจ — เซฟมี 6 ช่อง ถ้าเก็บระดับเสียงไว้ในเซฟ
## เสียงจะเปลี่ยนไปมาทุกครั้งที่ผู้เล่นโหลดคนละช่อง
func _save_settings() -> void:
	var cfg := ConfigFile.new()
	cfg.load(SETTINGS)
	cfg.set_value("audio", "master", _levels["Master"])
	cfg.set_value("audio", "sfx", _levels["SFX"])
	cfg.set_value("audio", "muted", muted)
	cfg.save(SETTINGS)


# ========== เล่นจริง ==========

func _play(id: String) -> void:
	if muted: return
	if not WQBank.SPEC.has(id): return
	var now := Time.get_ticks_msec() / 1000.0
	if now - float(_last.get(id, -99.0)) < COOLDOWN: return
	_last[id] = now
	played.append(id)
	if _silent or _players.is_empty(): return

	var st := _stream(id)
	if st == null: return
	var p := _players[_next]
	_next = (_next + 1) % _players.size()
	p.stream = st
	p.play()


## โหลดไฟล์ที่อบไว้ · ถ้าไฟล์หาย ตกกลับไปสังเคราะห์สดให้เกมยังมีเสียง
## (เกิดได้ตอนที่คนเพิ่ง clone repo แล้วยังไม่ได้รัน --import)
func _stream(id: String) -> AudioStream:
	if _cache.has(id): return _cache[id]
	var path := "%s/%s.wav" % [SFX_DIR, id]
	var st: AudioStream = load(path) if ResourceLoader.exists(path) else WQSynth.stream(WQBank.SPEC[id])
	_cache[id] = st
	return st
```

- [ ] **Step 4: ลงทะเบียน autoload ใน `project.godot`**

เพิ่มต่อจากหัวข้อ `[application]`:

```ini
[autoload]

WQAudio="*res://audio/audio.gd"
```

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅`

- [ ] **Step 6: พิสูจน์ว่า sim ตัวอื่นไม่พังเพราะ autoload ตัวใหม่**

```bash
cd godot
godot --headless --path . --script res://sim/headless_sim.gd
godot --headless --path . --script res://sim/ui_check.gd
godot --headless --path . --script res://sim/flow_check.gd
godot --headless --path . --script res://sim/world_check.gd
```

Expected: ผ่านทั้งสี่ตัวเหมือนเดิม

- [ ] **Step 7: commit**

```bash
git add godot/audio/audio.gd godot/project.godot godot/sim/audio_check.gd godot/audio/*.uid
git commit -m "Godot: WQAudio — autoload สองเลน เสียงเหตุการณ์มาจากสัญญาณ เสียง UI เรียกเอง"
```

---

### Task 5: ต่อสายเข้าเกมจริง

**Files:**
- Modify: `ui/main.gd` — ผูก `WQAudio` · เสียงเปิด/ปิดแผง · เสียงเซฟ/โหลด · `WQ_SFX`
- Modify: `ui/widgets/hud.gd` — เสียงคลิกปุ่มบน HUD
- Modify: `ui/widgets/action_panel.gd` — เสียง `denied`
- Modify: `ui/widgets/dice.gd` — `dice_roll` · `dice_land`
- Modify: `ui/screens/tutorial.gd` — `tutorial_step`
- Modify: `sim/audio_check.gd` — เพิ่ม `_check_wiring()`

**Interfaces:**
- Consumes: `WQAudio.bind()` · `WQAudio.bind_player()` · `WQAudio.ui()`
- Produces: ไม่มี API ใหม่ — งานต่อสายล้วน

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

เพิ่มใน `sim/audio_check.gd`:

```gdscript
## หน้าจอจริงต้องผูก WQAudio เข้ากับแมตช์ ไม่ใช่แค่มีระบบเสียงอยู่เฉยๆ
## นี่คือบั๊กแบบที่ flow_check เคยจับได้: ของมีครบทุกชิ้นแต่ไม่มีใครต่อสายให้
func _check_wiring() -> void:
	var main: Control = load("res://ui/Main.tscn").instantiate()
	root.add_child(main)
	await process_frame
	main._start_match("teacher", 0, 0)
	await process_frame

	_eq("main.gd ผูกแมตช์เข้ากับ WQAudio แล้ว", WQAudio._match, main.m)
	_eq("main.gd ผูกผู้เล่นเข้ากับ WQAudio แล้ว", WQAudio._player, main.m.players[0])

	# เปิดแผงบันทึกต้องมีเสียง
	WQAudio.played.clear()
	main._open_save_panel("save")
	await process_frame
	_eq("เปิดแผงมีเสียง", WQAudio.played.has("panel_open"), true)

	WQAudio.played.clear()
	main._close_save_panel()
	await process_frame
	_eq("ปิดแผงมีเสียง", WQAudio.played.has("panel_close"), true)

	main.free()
```

เรียกด้วย `await _check_wiring()` ต่อจาก `_check_audio()` ใน `_init()`

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `main.gd ผูกแมตช์เข้ากับ WQAudio แล้ว: ได้ <Object#null> ต้องการ ...`

- [ ] **Step 3: ผูก `WQAudio` ใน `ui/main.gd`**

ใน `_adopt_match()` (บรรทัด 132) ต่อจากบรรทัดที่ผูก `month_ended`:

```gdscript
	WQAudio.bind(m)
	WQAudio.bind_player(m.players[0])
```

ใน `_open_save_panel()` (บรรทัด 372) ต้นฟังก์ชัน:

```gdscript
	WQAudio.ui("panel_open")
```

ใน `_close_save_panel()` (บรรทัด 382) ต้นฟังก์ชัน:

```gdscript
	WQAudio.ui("panel_close")
```

ใน `_on_save_slot()` และ `_on_load_slot()` หลังเขียน/อ่านไฟล์สำเร็จ:

```gdscript
	WQAudio.ui("save")    # ใน _on_save_slot
	WQAudio.ui("load")    # ใน _on_load_slot
```

ใน `_ready()` ต่อจากบล็อก `WQ_SHOT` — ฟังเสียงทีละตัวจาก terminal:

```gdscript
	# ฟังเสียงทีละตัวจาก terminal: WQ_SFX=win godot --path .
	# มีเพราะ audio_check ตรวจได้แค่ว่ามีไฟล์และยิงถูกจังหวะ ไม่ได้ตรวจว่าฟังรู้เรื่อง
	# (บทเรียนเดียวกับที่ต้องเปิดดูไอคอนจริงทุกครั้งหลังอบ)
	var sfx := OS.get_environment("WQ_SFX")
	if sfx != "":
		await get_tree().create_timer(0.2).timeout
		WQAudio._play(sfx)
		await get_tree().create_timer(2.0).timeout
		get_tree().quit()
```

- [ ] **Step 4: เสียงคลิกในวิดเจ็ต**

`ui/widgets/hud.gd` — ปุ่มทั้งสามตัว:

```gdscript
	save_btn.pressed.connect(func(): WQAudio.ui("click"); save_pressed.emit())
	load_btn.pressed.connect(func(): WQAudio.ui("click"); load_pressed.emit())
	_end.pressed.connect(func(): WQAudio.ui("click"); end_turn_pressed.emit())
```

`ui/widgets/action_panel.gd` — `_do()` (บรรทัด 265) ต้องอ่านผลจาก core แล้วส่งเสียงเมื่อทำไม่ได้
ตอนนี้มันทิ้งค่าที่ core คืนมาทั้งหมด ผู้เล่นจึงกดปุ่มแล้วไม่มีอะไรตอบเลยเวลาทำไม่ได้:

```gdscript
func _do(act: String) -> void:
	var res := {}
	match act:
		"scout": res = _p.scout()
		"ot": res = _p.side_job("ot")
		"freelance": res = _p.side_job("freelance")
		"study": res = _p.study()
		"rest": res = _p.rest()
		"gym": res = _p.exercise()
		"resort": res = _p.vacation()
	# สำเร็จแล้วเสียงมาจากสัญญาณ acted ของ core เอง ที่นี่รับผิดชอบแค่ "ทำไม่ได้"
	if not bool(res.get("ok", true)): WQAudio.ui("denied")
```

`ui/widgets/dice.gd` — `WQAudio.ui("dice_roll")` ตอนเริ่มหมุน · `WQAudio.ui("dice_land")` ตอนหยุด
(ทั้งคู่อยู่ใน `WQBank.UI_IDS` ตั้งแต่ Task 1 แล้ว ไม่ต้องแก้ bank ที่นี่)

`world/city/city.gd` — **ไม่ต้องแก้** ตอนตัวละครเดินถึงที่หมายไม่มีเสียง เพราะแอนิเมชันเดินข้ามได้
(กฎ `world/` ข้อ 4) เสียง "ถึงแล้ว" จะดังบ้างไม่ดังบ้างโดยผู้เล่นไม่มีทางรู้ว่าทำไม —
การเดินทางมีเสียงเดียวคือ `travel` ตอนกดปุ่ม ซึ่งเป็นจังหวะที่ core เปลี่ยนสถานะจริง

`ui/screens/tutorial.gd` — ตอนขึ้นสเต็ปใหม่: `WQAudio.ui("tutorial_step")`

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅`

- [ ] **Step 6: รัน sim ทั้งชุด**

```bash
cd godot
for s in rng_check headless_sim save_check ui_check flow_check world_check; do
  godot --headless --path . --script res://sim/$s.gd || echo "FAILED: $s"
done
```

Expected: ไม่มีบรรทัด `FAILED:` โผล่มา

- [ ] **Step 7: commit**

```bash
git add godot/ui godot/sim/audio_check.gd
git commit -m "Godot: ต่อเสียงเข้าเกมจริง — สัญญาณของ core · คลิก UI · แผงเปิดปิด · ลูกเต๋า"
```

---

### Task 6: แผงปรับเสียง + จำค่าไว้ข้ามเกม

**Files:**
- Create: `ui/widgets/audio_panel.gd`
- Modify: `ui/widgets/hud.gd` — ปุ่ม 🔊 + สัญญาณ `audio_pressed`
- Modify: `ui/main.gd` — เปิด/ปิดแผงปรับเสียง
- Modify: `sim/audio_check.gd` — เพิ่ม `_check_settings()`

**Interfaces:**
- Consumes: `WQAudio.set_level()` · `WQAudio.get_level()` · `WQAudio.set_muted()` · `WQAudio.muted`
- Produces:
  - `WQAudioPanel` — `class_name`, `extends PanelContainer`, `signal closed`
  - `WQHud.audio_pressed` — สัญญาณใหม่

- [ ] **Step 1: เขียนเทสต์ที่ต้องล้มเหลวก่อน**

```gdscript
## ระดับเสียงต้องอยู่ข้ามการเปิดเกมใหม่ และต้อง **ไม่** อยู่ในไฟล์เซฟ
## (เซฟมี 6 ช่อง ถ้าเก็บในเซฟ เสียงจะเปลี่ยนไปมาตามช่องที่โหลด)
func _check_settings() -> void:
	WQAudio.set_level("SFX", 0.3)
	WQAudio.set_muted(true)

	var cfg := ConfigFile.new()
	_eq("เขียน settings.cfg แล้ว", cfg.load(WQAudio.SETTINGS), OK)
	_eq("จำระดับเสียง SFX", float(cfg.get_value("audio", "sfx", -1.0)), 0.3)
	_eq("จำสถานะปิดเสียง", bool(cfg.get_value("audio", "muted", false)), true)

	# โหลดกลับมาต้องได้ค่าเดิม
	WQAudio._levels["SFX"] = 1.0
	WQAudio.muted = false
	WQAudio._load_settings()
	_eq("โหลดค่าเดิมกลับมาได้", WQAudio.get_level("SFX"), 0.3)
	_eq("โหลดสถานะปิดเสียงกลับมาได้", WQAudio.muted, true)

	WQAudio.set_muted(false)
	WQAudio.set_level("SFX", 1.0)

	var panel := WQAudioPanel.new()
	root.add_child(panel)
	await process_frame
	_eq("สไลเดอร์ตั้งค่าตามที่ WQAudio จำไว้", panel._sfx.value, 1.0)
	panel._sfx.value = 0.5
	await process_frame
	_eq("ขยับสไลเดอร์แล้วเสียงเปลี่ยนจริง", WQAudio.get_level("SFX"), 0.5)
	WQAudio.set_level("SFX", 1.0)
	panel.free()
```

- [ ] **Step 2: รันให้เห็นว่าล้มเหลวจริง**

```bash
cd godot && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: FAIL — `Identifier "WQAudioPanel" not declared in the current scope`

- [ ] **Step 3: เขียน `ui/widgets/audio_panel.gd`**

```gdscript
class_name WQAudioPanel
extends PanelContainer
## แผงปรับเสียง — สไลเดอร์สองตัวกับปุ่มปิดเสียง ไม่มีอะไรมากกว่านั้น
##
## ค่าที่ขยับเขียนลง user://settings.cfg ทันที ไม่ต้องกดตกลง เพราะผู้เล่นปรับเสียง
## ระหว่างที่เกมกำลังเล่นอยู่ ต้องได้ยินผลทันทีที่ลาก

signal closed

var _master: HSlider
var _sfx: HSlider
var _mute: CheckBox


func _init() -> void:
	var box := VBoxContainer.new()
	box.add_theme_constant_override("separation", 10)
	add_child(box)

	var title := Label.new()
	title.text = "🔊 เสียง"
	title.add_theme_font_size_override("font_size", 16)
	box.add_child(title)

	_master = _slider(box, "เสียงรวม", WQAudio.get_level("Master"))
	_master.value_changed.connect(func(v: float): WQAudio.set_level("Master", v))
	_sfx = _slider(box, "เอฟเฟกต์", WQAudio.get_level("SFX"))
	_sfx.value_changed.connect(func(v: float): WQAudio.set_level("SFX", v))

	_mute = CheckBox.new()
	_mute.text = "ปิดเสียงทั้งหมด"
	_mute.button_pressed = WQAudio.muted
	_mute.toggled.connect(func(on: bool): WQAudio.set_muted(on))
	box.add_child(_mute)

	var close := Button.new()
	close.text = "ปิด"
	close.pressed.connect(func(): WQAudio.ui("panel_close"); closed.emit())
	box.add_child(close)


func _slider(box: VBoxContainer, label: String, value: float) -> HSlider:
	var l := Label.new()
	l.text = label
	box.add_child(l)
	var s := HSlider.new()
	s.min_value = 0.0
	s.max_value = 1.0
	s.step = 0.05
	s.value = value
	s.custom_minimum_size.x = 200
	box.add_child(s)
	return s
```

- [ ] **Step 4: เพิ่มปุ่ม 🔊 ใน `ui/widgets/hud.gd`**

เพิ่มสัญญาณใต้ `signal load_pressed`:

```gdscript
signal audio_pressed
```

เพิ่มปุ่มก่อน `save_btn` (ปุ่มจบตาต้องอยู่ริมสุดตัวเดียวตามคอมเมนต์เดิมในไฟล์):

```gdscript
	var audio_btn := Button.new()
	audio_btn.text = "🔊"
	audio_btn.tooltip_text = "ปรับเสียง"
	audio_btn.pressed.connect(func(): WQAudio.ui("click"); audio_pressed.emit())
	row.add_child(audio_btn)
```

ใน `ui/main.gd` `_build_layout()` ต่อจาก `hud.load_pressed.connect(...)`:

```gdscript
	hud.audio_pressed.connect(_open_audio_panel)
```

และเพิ่มสองฟังก์ชัน:

```gdscript
var audio_panel: WQAudioPanel     ## แผงปรับเสียง — มีอยู่แปลว่ากำลังเปิดค้างอยู่


func _open_audio_panel() -> void:
	if audio_panel != null: return
	WQAudio.ui("panel_open")
	audio_panel = WQAudioPanel.new()
	audio_panel.set_anchors_preset(Control.PRESET_CENTER)
	audio_panel.closed.connect(_close_audio_panel)
	add_child(audio_panel)


func _close_audio_panel() -> void:
	if audio_panel == null: return
	audio_panel.queue_free()
	audio_panel = null
```

- [ ] **Step 5: รันเทสต์ให้ผ่าน**

```bash
cd godot && godot --headless --path . --import && godot --headless --path . --script res://sim/audio_check.gd
```

Expected: `audio_check: ผ่านทั้งหมด ✅`

- [ ] **Step 6: ดูของจริงด้วยตา**

```bash
cd godot && WQ_JOB=teacher WQ_TUT=off WQ_SHOT=/tmp/audio_ui.png godot --path .
```

เปิด `/tmp/audio_ui.png` ดูว่าปุ่ม 🔊 อยู่บน HUD จริงและไม่ดันปุ่มจบตาตกขอบ

- [ ] **Step 7: commit**

```bash
git add godot/ui/widgets/audio_panel.gd godot/ui/widgets/hud.gd godot/ui/main.gd godot/sim/audio_check.gd godot/ui/widgets/*.uid
git commit -m "Godot: แผงปรับเสียง — สไลเดอร์สองตัว ปิดเสียงได้ จำค่าไว้ข้ามเกม"
```

---

### Task 7: เช็กลิสต์ · ฟังจริงทุกตัว · เอกสาร

**Files:**
- Create: `audio/tools/sfx_readme.gd`
- Create: `audio/README.md` (ผลจากสคริปต์)
- Modify: `godot/CLAUDE.md` — กฎใหม่ 4 ข้อ + ตารางไฟล์ + สถานะ
- Modify: `godot/README.md` — คำสั่ง + ตารางสถานะ

**Interfaces:**
- Consumes: `WQBank.ids()` · `audio/sfx/baked.json`
- Produces: `audio/README.md` — เช็กลิสต์ให้คนทำเสียง

- [ ] **Step 1: เขียน `audio/tools/sfx_readme.gd`**

```gdscript
extends SceneTree
## สร้าง audio/README.md ใหม่จากรายการจริงใน WQBank — **ห้ามแก้ README ด้วยมือ**
##   godot --headless --path . --script res://audio/tools/sfx_readme.gd
## กติกาเดียวกับ world/tools/models_readme.gd: เช็กลิสต์ที่พิมพ์ด้วยมือจะโกหกภายในสัปดาห์เดียว

const OUT := "res://audio/README.md"
const STAMP := "res://audio/sfx/baked.json"


func _init() -> void:
	var marks := {}
	var f := FileAccess.open(STAMP, FileAccess.READ)
	if f != null:
		var parsed = JSON.parse_string(f.get_as_text())
		if parsed is Dictionary: marks = parsed
		f.close()

	var lines: Array[String] = []
	lines.append("# เสียงของ Wealth Quest")
	lines.append("")
	lines.append("> ไฟล์นี้สร้างจาก `audio/tools/sfx_readme.gd` **ห้ามแก้ด้วยมือ**")
	lines.append("")
	lines.append("ทุกไฟล์ตอนนี้เป็นคลื่นสังเคราะห์จาก `WQSynth` ไม่ใช่เสียงที่คนทำ")
	lines.append("วางไฟล์ `.wav` ของตัวเองทับได้ทีละตัว — ตัวอบจะเห็นว่า sha256 ไม่ตรงกับที่จดไว้")
	lines.append("แล้วจะไม่แตะไฟล์นั้นอีกเลย")
	lines.append("")
	lines.append("| id | คลื่น | ยาว (วิ) | ที่มา |")
	lines.append("|---|---|---|---|")

	var human := 0
	for id in WQBank.ids():
		var spec: Array = WQBank.SPEC[id]
		var path := "res://audio/sfx/%s.wav" % id
		var abs := ProjectSettings.globalize_path(path)
		var src := "❓ ยังไม่มีไฟล์"
		if FileAccess.file_exists(abs):
			if FileAccess.get_sha256(abs) == String(marks.get(id, "")):
				src = "🤖 อบจากโค้ด"
			else:
				src = "🙌 คนทำ"
				human += 1
		lines.append("| `%s` | %s | %.2f | %s |" % [id, String(spec[0]), float(spec[3]), src])

	lines.append("")
	lines.append("**คนทำเสียงจริงแล้ว %d/%d ตัว**" % [human, WQBank.ids().size()])
	lines.append("")
	lines.append("## สเปกสำหรับคนทำเสียง")
	lines.append("")
	lines.append("- mono · 22050 Hz ขึ้นไป · 16-bit PCM `.wav`")
	lines.append("- ยาวไม่เกิน 2 วินาที (เสียงชนะยาวสุดที่ 1.2 วินาที)")
	lines.append("- ชื่อไฟล์ = id ในตารางข้างบนเป๊ะ")
	lines.append("- ทำให้ดังพอๆ กับไฟล์ที่อบไว้ ระบบไม่มี normalize ให้")

	var out := FileAccess.open(OUT, FileAccess.WRITE)
	out.store_string("\n".join(lines) + "\n")
	out.close()
	print("sfx_readme: เขียน %s (คนทำแล้ว %d ตัว)" % [OUT, human])
	quit()
```

- [ ] **Step 2: สร้าง README แล้วดูผล**

```bash
cd godot && godot --headless --path . --script res://audio/tools/sfx_readme.gd && cat audio/README.md
```

Expected: ตารางครบทุก id · ทุกแถวเป็น `🤖 อบจากโค้ด` · บรรทัดสรุป `คนทำเสียงจริงแล้ว 0/30 ตัว`

- [ ] **Step 3: ฟังเสียงจริงทุกตัว**

```bash
cd godot && for id in $(godot --headless --path . --script res://audio/tools/sfx_readme.gd >/dev/null 2>&1; \
  grep -o '^| `[a-z_]*`' audio/README.md | tr -d '|` '); do
  echo "--- $id ---"; WQ_SFX=$id godot --path .
done
```

ฟังทีละตัวแล้วจดว่าตัวไหน "ฟังไม่ออกว่าคืออะไร" — บทเรียนจากรอบอบไอคอนคือของหกชิ้นแรก
ต้องรื้อทำใหม่หลังเห็นของจริง **ตัวตรวจอัตโนมัติจับเรื่องนี้ไม่ได้เลย**
ถ้าตัวไหนต้องแก้: แก้ค่าใน `WQBank.SPEC` → รัน `sfx_bake` → `--import` → ฟังใหม่

- [ ] **Step 4: อัปเดต `godot/CLAUDE.md`**

เพิ่มในหัวข้อ "กฎเหล็กของโปรเจกต์":

```markdown
6. **เสียงของเหตุการณ์ในเกมมาจากสัญญาณเท่านั้น** — `ui/` เรียกได้แค่ `WQAudio.ui()`
   ซึ่งรับเฉพาะ id ใน `WQBank.UI_IDS` (เสียง UI ล้วน) ถ้าปล่อยให้ `ui/` ยิงเสียงเหตุการณ์เองได้
   วันหนึ่งจะมีเสียง "ปิดดีลสำเร็จ" ดังตอนที่เงินไม่พอ
7. **เพิ่มเมธอดที่เปลี่ยน state ใน `core/player.gd` ต้องยิง `changed` และ `acted(kind)` เสมอ**
   และ **ห้ามยิง `acted` ตอนคืน `_fail()`**
```

เพิ่มตารางไฟล์ใหม่ในหัวข้อ "เพิ่มตอนทำข้อ 9 (เสียง)":

```markdown
### เพิ่มตอนทำข้อ 9 (เสียง)

| ไฟล์ | หน้าที่ |
|---|---|
| `audio/synth.gd` | `WQSynth` — สังเคราะห์ square/triangle/noise **ต้องได้ไบต์เดิมทุกครั้ง** (noise ใช้ seed คงที่) |
| `audio/bank.gd` | `WQBank` — สเปกเสียงทุกตัว + ตารางสัญญาณ→เสียง ที่เดียวของทั้งระบบ |
| `audio/audio.gd` | `WQAudio` — autoload ตัวเดียวของโปรเจกต์ · สองเลน · pool 8 · ระดับเสียง |
| `audio/tools/sfx_bake.gd` | อบเป็น `.wav` จริง + จด sha256 กันทับงานคนทำเสียง |
| `audio/tools/sfx_readme.gd` | สร้าง `audio/README.md` (**ห้ามแก้ด้วยมือ**) |
| `ui/widgets/audio_panel.gd` | `WQAudioPanel` — สไลเดอร์ Master/SFX + ปิดเสียง เก็บที่ `user://settings.cfg` |
| `sim/audio_check.gd` | เทสต์ headless ของทั้งระบบเสียง |
```

เพิ่มในหัวข้อ "ที่ของจริงต่างจาก ART-DIRECTION / CLAUDE-CODE-BRIEF":

```markdown
- **`arrive` ถูกยุบทิ้ง** — เสียง "เดินถึงที่หมาย" ดังบ้างไม่ดังบ้างเพราะแอนิเมชันเดินข้ามได้
  (กฎ `world/` ข้อ 4) ผู้เล่นจะไม่มีทางรู้ว่าทำไมบางครั้งเงียบ · การเดินทางมีเสียงเดียวคือ `travel`
  ตอนกดปุ่ม ซึ่งเป็นจังหวะที่ core เปลี่ยนสถานะจริง
- **บัส `SFX` สร้างจากโค้ดใน `WQAudio._ensure_bus()`** ไม่ได้ใช้ `default_bus_layout.tres`
  เพราะ `--headless` เริ่มมาด้วย `bus_count = 1` การสร้างเองทำให้ทุกโหมดเหมือนกันและเทสต์ได้
- **`AudioStreamWAV.save_to_wav()` ไม่มีช่องใส่ metadata** ต่างจาก glTF ที่มี `copyright`
  ตราประทับ "อบจากโค้ด" จึงอยู่ใน `audio/sfx/baked.json` เป็น sha256 ต่อไฟล์แทน
```

- [ ] **Step 5: อัปเดต `godot/README.md`**

เพิ่มในบล็อกคำสั่ง:

```bash
godot --headless --path . --script res://sim/audio_check.gd     # 7) ระบบเสียง
godot --headless --path . --script res://audio/tools/sfx_bake.gd   # อบเสียงเป็น .wav
godot --headless --path . --script res://audio/tools/sfx_readme.gd # สร้าง audio/README.md
WQ_SFX=win godot --path .                                       # ฟังเสียงตัวเดียวแล้วปิดเอง
WQ_MUTE=1 godot --path .                                        # เปิดเกมแบบไม่มีเสียง
```

แก้แถว "เสียง" ในตารางสถานะ จาก `⬜ ยังไม่มี` เป็น:

```markdown
| เสียง | ✅ **30 เสียงครบทุกเหตุการณ์** สังเคราะห์เอง อบเป็น `.wav` จริง · ปรับ/ปิดได้จาก HUD (เสียงที่คนทำจริงยัง 0/30) |
```

และแก้หัวข้อ "ที่ยังค้างอยู่" — ลบบรรทัด "ยังไม่มีเสียง" ออก เพิ่มแทน:

```markdown
- **ยังไม่มีเสียงที่คนทำจริงสักไฟล์** — 0/30 ทุกตัวเป็นคลื่นสังเคราะห์จากโค้ด
  ท่อเดินครบวงแล้ว วางไฟล์ทับได้ทีละตัว (เช็กลิสต์ใน `audio/README.md`)
- ยังไม่มีเพลงพื้นหลัง (บัสเผื่อช่อง `Music` ไว้แล้ว)
```

- [ ] **Step 6: รันทุกอย่างครั้งสุดท้าย**

```bash
cd godot
godot --headless --quit
for s in rng_check headless_sim save_check ui_check flow_check world_check audio_check; do
  godot --headless --path . --script res://sim/$s.gd || echo "FAILED: $s"
done
godot --headless --path . --script res://sim/parity_dump.gd -- 60
WQ_JOB=teacher WQ_TUT=off WQ_SHOT=/tmp/final.png godot --path .
```

Expected: ไม่มี `FAILED:` · `parity_dump` ยังตรงกับ `engine.js` ทุกช่อง · เปิด `/tmp/final.png` ดูแล้วหน้าจอปกติ

- [ ] **Step 7: commit**

```bash
git add godot/audio godot/CLAUDE.md godot/README.md
git commit -m "Godot: เช็กลิสต์เสียง + เอกสาร — ระบบเสียงครบวง (ข้อ 9)"
```

---

## Self-Review

**ครอบคลุม spec ครบไหม:**

| หัวข้อใน spec | Task |
|---|---|
| สังเคราะห์เอง อบเป็น `.wav` | 1, 2 |
| ตราประทับ sha256 กันทับงานคน | 2 |
| `acted(kind)` + บอทเงียบ + parity | 3, 4 |
| `WQAudio` autoload สองเลน + pool + cooldown + headless | 4 |
| เสียงครบทุกเหตุการณ์ | 1 (30 ตัว — `arrive` ยุบทิ้งตั้งแต่ต้น ดูเหตุผลข้างล่าง) |
| ปุ่มปรับเสียง + `user://settings.cfg` แยกจากเซฟ | 6 |
| `sim/audio_check.gd` ครบ 7 ข้อ | 1–6 |
| `WQ_SFX=<id>` ฟังจริง | 5, 7 |
| กฎ 4 ข้อลง `CLAUDE.md` | 7 |
| ที่ไม่ทำรอบนี้ (เพลง · เสียง 3D · ducking) | ไม่มี task — ตั้งใจตัด |

**ที่ต่างจาก spec (ตั้งใจ พร้อมเหตุผล):**

1. **`arrive` ยุบทิ้งตั้งแต่ Task 1** เหลือ 30 เสียง — แอนิเมชันเดินข้ามได้ตามกฎ `world/` ข้อ 4 เสียงจะดังบ้างไม่ดังบ้างโดยผู้เล่นไม่รู้สาเหตุ
2. **`dice_roll` / `dice_land` อยู่เลน UI ตั้งแต่ต้น** — ลูกเต๋าเป็นภาพเคลื่อนไหว แต้มมาจาก core ไปแล้วก่อนหมุน จึงไม่ใช่เหตุการณ์ที่เปลี่ยนสถานะ
3. **ไม่ใช้ `default_bus_layout.tres`** — สร้างบัสจากโค้ด เพราะ headless เริ่มมาด้วยบัสเดียว
4. **`claim_dream` ยิง `acted("dream")` แต่ไม่มีเสียง** — กันเสียงชนะดังซ้อนกับ `player_finished`
5. **`month_end` กับ `payday` ดังต่อกันได้ในเดือนเดียว** — `month_end` คือ "เดือนจบแล้ว" `payday` คือ "เดือนนี้เหลือเก็บ" คนละความหมาย ห่างกันพอเพราะ cooldown แยกตาม id
